# 시뮬레이션 디자인 시스템 v4.0
## "Classroom Instrument — Web" · 화학 탐구실 웹사이트 전용 기준

> **v3.0과 무엇이 다른가**
> v3.0은 *혼자 열리는 단일 HTML 파일*을 전제로 만들어졌습니다. 그래서 화면 전체를
> 차지하는 TopBar·사이드패널·StatusBar를 스스로 그리고, `:root` 토큰도 파일마다 새로
> 선언했습니다. 파일이 하나뿐일 때는 그게 맞습니다.
>
> 이 사이트는 다릅니다. **여러 시뮬레이션이 공통 껍데기를 나눠 씁니다.**
> 상단 이동 메뉴는 `shared/nav.js`가, 색·버튼·슬라이더는 `shared/style.css`가 이미
> 담당합니다. 각 시뮬레이션이 자기 토큰을 다시 선언하면 페이지마다 색이 미세하게
> 어긋나고, 나중에 색 하나 바꾸려면 파일을 전부 열어야 합니다.
>
> v4.0은 **"페이지는 앱이 아니라 문서다"** 라는 전제로 v3.0을 다시 쓴 것입니다.
> 색 철학(밝은 교실·빔프로젝터·인쇄 연계)은 그대로 계승하되,
> 레이아웃·토큰 관리·검증 방법을 웹사이트 구조에 맞게 바꿨습니다.
>
> 작성 2026-07-28 · 대상 `화학탐구실/` 저장소 · 선행 문서 v3.0(단일 파일용, 계속 유효)

---

## 이 문서를 쓰는 법

새 시뮬레이션을 만들 때 아래 **「복사용 프롬프트 블록」** 을 통째로 복사해 프롬프트 앞에
붙이고, 뒤에 "무엇을 만들지"만 적으면 됩니다.

```
[여기에 복사용 프롬프트 블록 붙여넣기]

위 기준에 따라 '산-염기 적정' 시뮬레이션을 만들어 줘.
0.1 M NaOH로 0.1 M HCl 20 mL를 적정하는 상황이고,
학생이 조절할 변인은 적하 속도와 지시약 종류야.
```

---

# 📋 복사용 프롬프트 블록 v4.0

```
[🔬 디자인 시스템 v4.0 — 화학 탐구실 웹사이트 시뮬레이션 기준]

이 시뮬레이션은 '화학 탐구실' 사이트의 한 페이지로 들어간다.
혼자 떠 있는 앱이 아니라 사이트의 일부다. 아래 기준을 따른다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§1. 기술 제약 — 타협 불가
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 프레임워크·빌드 도구·npm 금지. 순수 HTML + CSS + JS + Canvas.
- fetch()·ES 모듈(import/export)·type="module" 금지.
  → 쓰면 file:// 에서 CORS로 막혀 더블클릭으로 안 열린다. 취향이 아니라 요구사항이다.
  → 스크립트는 반드시 일반 <script src="..."></script> 로만 불러온다.
- 외부 CDN·웹폰트·이미지 파일 금지. 아이콘은 인라인 SVG로 그린다.
  → 학교 인터넷 필터링에 CDN이 막히면 수업이 통째로 멈춘다.
- localStorage 금지. 상태는 JS 변수로만 들고 있는다.
- 폴더 구조: 새 시뮬레이션은 영문 소문자 폴더 하나
  (예: titration/) 안에 index.html + sim.js 두 파일로 나눈다.
  화면(HTML)과 물리(JS)를 섞지 않는다.
- 페이지 맨 아래 3줄은 고정 형식:
    <script src="../sims.js"></script>
    <script src="../shared/nav.js" data-base=".." data-current="새id"></script>
    <script src="sim.js"></script>
- 등록: ../sims.js 의 해당 항목을 ready:true 로. 대문 카드와 이동 메뉴는 자동 생성된다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§2. 설계 철학 — 6원칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① 광량 위계 (Luminance Hierarchy)
   화면에서 시선이 가장 먼저 닿아야 하는 곳은 오직 하나 — 관찰 영역(무대)이다.
   무대는 주변 면과 명도가 확실히 갈려야 한다(§5). 나머지는 전부 조용한 회색 계열.

② 데이터 중심 (Data-Driven Display)
   장식용 그라데이션·그림자·아이콘을 넣지 않는다.
   화면의 모든 픽셀은 "학생이 읽어야 할 값"이거나 "그 값을 읽게 돕는 것"이어야 한다.

③ 물리적 정확성 (Physical Fidelity)
   보이는 것이 곧 계산 결과여야 한다. 예쁘게 보이려고 값을 보정하지 않는다.
   근사·가정·무시한 항은 화면이나 활동지에 명시한다.

④ 암묵적 안내 (Implicit Scaffolding)
   "이렇게 하세요" 설명문 대신 인터페이스 자체로 유도한다.
   - 슬라이더 범위를 교육적으로 의미 있는 구간으로만 제한한다.
     (아무 값이나 되면 학생은 의미 없는 구간에서 시간을 쓴다)
   - 첫 화면(기본값)이 이미 "볼 만한 상태"여야 한다. 빈 화면으로 시작하지 않는다.
   - 학생이 무엇을 하든 망가지지 않게 한다. 되돌리기 버튼보다 안전한 범위가 낫다.

⑤ 신호 주기 (Signaling)
   지금 중요한 것 하나만 강조한다. 값이 바뀐 순간에만 짧게 표시가 뜨고 사라진다.
   상시로 켜져 있는 강조는 강조가 아니라 배경이다.

⑥ 끊어 주기 (Segmenting)
   긴 과정은 학생이 스스로 넘기는 단계로 자른다.
   자동으로 흘러가는 긴 애니메이션은 이해를 돕지 않는다. 일시정지·단계 이동을 준다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§3. 페이지 레이아웃 표준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
페이지는 "화면을 꽉 채우는 앱"이 아니라 "위에서 아래로 읽는 문서"다.
100vh·position:fixed·overflow:hidden 을 쓰지 않는다.
(태블릿 주소창이 나타났다 사라지면서 100vh가 흔들려 아래가 잘린다)

세로 순서 — 이 순서를 지킨다:

  [nav.js가 자동 삽입하는 상단 메뉴]     ← 직접 만들지 않는다
  ├ 1. 제목 (h1) + 한 줄 설명
  ├ 2. 이름 입력 줄            (학생 제출용. 선택 사항이면 생략 가능)
  ├ 3. ★ 무대 + 조절 변인 ★    ← 화면의 중심. 좌우 2단
  │      좌: 관찰 영역(캔버스)
  │      우: 조절 변인 슬라이더·체크박스
  ├ 4. 측정값 카드들           (무대 바로 아래 — 원인과 결과를 가까이)
  ├ 5. 그래프·기록표 + CSV 내려받기
  └ 6. 활동지 (질문 5~8개)

2단 → 1단 전환 기준: 1024px 미만에서 세로 1단으로 바꾼다.
  @media (max-width:1023px){ .layout{ grid-template-columns:1fr } }
1단이 되면 순서는 무대 → 조절 변인 → 측정값 이어야 한다.
조절 변인이 무대보다 위로 올라가면 학생은 무엇이 바뀌는지 못 본 채 슬라이더를 만진다.

공간 근접 원칙: 어떤 값의 설명은 그 값 옆에 둔다.
  나쁨 — 캔버스 아래 "① 파란 입자는 가벼운 기체입니다" 같은 번호 매긴 범례 목록
  좋음 — 캔버스 안 또는 바로 옆에 색 점과 이름을 붙인 범례(.legend)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§4. 컬러 — 토큰만 쓴다
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
★ 페이지에서 :root 를 다시 선언하지 않는다. shared/style.css 의 토큰을 그대로 쓴다.
   색이 마음에 안 들면 페이지가 아니라 shared/style.css 를 고친다.
   그래야 사이트 전체가 함께 바뀐다.

/* 면 */
--bg     #f5f7fa   페이지 배경 (순백 금지 — 장시간 응시 시 눈부심)
--panel  #ffffff   카드·패널
--line   #e3e6ea   구분선

/* 글자 — 괄호는 흰 카드 위 실측 대비비 */
--t1 #1f2328  제목·핵심 수치        (15.80:1)
--t2 #454b52  본문·레이블            (8.82:1)
--t3 #5b636b  11px 이하 소형 레이블   (6.10:1)
  ⚠ 소형 레이블에 #94a3b8 류의 연한 회색을 쓰지 않는다. 빔프로젝터로 쏘면 사라진다.

/* 데이터 컬러 — 밝은 면(카드·그래프·표) 위 */
--d-blue   #1d4ed8 (6.70:1)  --d-cyan   #0f5c8c (7.17:1)
--d-green  #15803d (5.02:1)  --d-amber  #b45309 (5.02:1)
--d-red    #b91c1c (6.47:1)  --d-violet #6d28d9 (7.10:1)
--d-gray   #5f6b7a (5.43:1)
  전부 흰 배경에서 WCAG AA(4.5:1) 이상 — 실측값이다.
  의미 배정: 파랑=기본계열 / 청록=보조·강조 / 초록=안정·정상 /
            황갈=주의·중간 / 적=경고·위험 / 보라=특수 / 회=중성·비활성

/* 어두운 무대 위 발광 계열 — 괄호는 #0f172a 대비 실측비 */
--p-sky #56b4e9 (7.74) --p-orange #fb923c (7.89) --p-yellow #facc15 (11.66)
--p-mint #34d399 (9.29) --p-violet #a78bfa (6.56) --p-silver #cbd5e1 (12.02)
  ⚠ 밝은 면용 색을 어두운 무대에 그대로 쓰지 않는다. 반대도 마찬가지다.
     #1d4ed8 을 #0f172a 무대에 찍으면 거의 검게 보인다.

/* CPK 원자색 — 국제 표준. 값 변경 금지 */
H #FFFFFF / C #404040 / N #3050F8 / O #FF0D0D / F #90E050
Cl #1FF01F / Na #AB5CF2 / S #FFFF30 / P #FF8000
  밝은 무대에서 H·S·Cl 는 외곽선이 없으면 사라진다.
  규칙: stroke = fill 을 40~60% 어둡게 한 색, 알파 0.85, lineWidth 1. 예외 없음.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§5. 무대(관찰 영역)는 두 종류다 — 고르는 규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
v3.0은 무대를 항상 순백으로 규정했다. 웹사이트에는 주제가 다양하므로
두 모드를 두고 주제에 따라 고른다. 한 페이지 안에서 섞지는 않는다.

■ 어두운 무대  .stage.stage--dark   배경 --stage-dark #0f172a
   쓸 때: 입자 운동, 빛·스펙트럼, 궤적, 전자·오비탈, 우주·천체
   이유:  작고 밝은 점이 주인공일 때 어두운 바탕이 가장 잘 보인다.
   위 --p-* 발광 계열을 쓴다. 격자선은 rgba(148,163,184,0.14).

■ 밝은 무대  .stage.stage--light   배경 --stage-light #ffffff
   쓸 때: 유리기구·용액·적정·침전·색변화·pH·용해도
   이유:  실제 실험은 밝은 곳에서 흰 배경에 대고 관찰한다. 학습지 인쇄와도 맞는다.
   화면에서 유일한 순백이어야 한다(그래서 페이지 배경이 #f5f7fa 인 것이다).
   격자선은 rgba(40,45,52,0.055). 유리 하이라이트는 흰색이 아니라
   옅은 청색 rgba(160,200,228,0.75) — 흰 안에 흰 선을 그으면 안 보인다.

캔버스 필수 처리(두 모드 공통):
  const dpr = Math.min(window.devicePixelRatio || 1, 2);   // 2 초과는 성능만 먹는다
  canvas.width  = wrap.clientWidth  * dpr;
  canvas.height = wrap.clientHeight * dpr;
  canvas.style.width = wrap.clientWidth + 'px';
  ctx.setTransform(dpr,0,0,dpr,0,0);
  → 이 처리를 빼면 태블릿에서 글자와 원이 뭉개진다.
  → ResizeObserver 로 폭 변화 때 다시 잡는다. 창 회전 시 필수.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§6. 타이포그래피
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 폰트는 shared/style.css 의 body 스택을 상속받는다. 페이지에서 다시 지정하지 않는다.
- 크기 위계: 제목 20~22 / 소제목 14 / 본문 13.5 / 레이블 12 / 소형 11
- 측정 수치는 22px, font-weight 600, font-variant-numeric:tabular-nums.
  ⚠ tabular-nums 없이 실시간 수치를 띄우면 숫자 폭이 달라져 값이 덜덜 떨린다.
- 밝은 배경 위에서 font-weight 300(얇은 글꼴)을 쓰지 않는다. 획이 사라진다.
  수치·화학식은 400~600.
- 화학식 아래첨자는 <sub> 로. 유니코드 ₂ 는 폰트에 따라 크기가 널뛴다.
  H<sub>2</sub>O 처럼 쓰고 CSS 로 font-size:.75em; vertical-align:-.15em 지정.
- 단위는 수치와 다른 크기·색으로 분리한다: <span class="value">564.3</span>
  <span class="unit">a.u.</span> — 단위가 수치와 같은 굵기면 자릿수로 착각한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§7. 조작 요소 규범
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 슬라이더 — 물리량마다 색을 배정한다.
   같은 파란 슬라이더가 5개면 학생은 매번 레이블을 읽어야 한다. 색이 다르면 위치로 안다.
     온도      class="rng-temp"    한색→난색 4단 그라데이션 (물리적 의미)
     부피·공간  class="rng-vol"     연파랑→진파랑
     개수·농도  class="rng-count"   연보라→진보라
     시간·속도  class="rng-time"    연초록→진초록
   레이블과 현재값은 슬라이더 바로 위 같은 줄에 좌·우로 배치한다.
   현재값에는 반드시 단위를 붙인다. "485" 가 아니라 "485 K".

■ 버튼 — 화면에 primary(파란 채움) 버튼은 최대 1개.
   나머지는 흰 배경 + 테두리. 파란 버튼이 3개면 무엇을 눌러야 할지 알 수 없다.

■ 체크박스 — 라벨 전체가 클릭 영역이어야 한다. <label> 로 감싼다.

■ 터치 크기 — shared/style.css 의 @media (pointer:coarse) 가 이미 처리한다.
   버튼 44px, 입력창 16px 글자, 슬라이더 손잡이 30px.
   ★ 페이지 <style> 은 공통 CSS보다 뒤에 오므로 같은 특이도면 페이지가 이긴다.
     터치 규칙을 덮어쓰지 않도록 페이지에서 button{font-size:...} 같은
     요소 선택자를 쓰지 않는다. 반드시 클래스로 좁혀서 쓴다.
   WCAG 2.2 (2.5.8)은 최소 24×24px, 애플·구글 권장은 44×44px.
   이 사이트는 학생 태블릿이 주 사용 환경이므로 44px 기준을 쓴다.

■ 키보드 — 슬라이더·버튼은 Tab 으로 닿아야 한다. tabindex="-1" 남발 금지.
   :focus-visible 링은 공통 CSS에 이미 있다. outline:none 으로 지우지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§8. 측정값 표시
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
<div class="readout">
  <div class="label">압력 P</div>
  <div><span class="value">564.3</span><span class="unit">a.u.</span></div>
</div>
상태색은 클래스로: .is-ok(초록) / .is-warn(주황) / .is-bad(빨강)
  ⚠ 상태를 색으로만 알리지 않는다. 짧은 글자를 함께 넣는다("정상" / "과열").

유효숫자를 고정한다. 소수점 자리가 3→4→3 으로 흔들리면 값이 튀어 보인다.
값이 임의 단위면 "a.u." 를 붙이고, 실제 단위가 아님을 활동지에서 한 번 설명한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§9. 색각·접근성 — 색 개수에 따라 규칙이 다르다
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
아래는 Viénot–Brettel–Mollon 색각이상 시뮬레이션 후 CIELAB ΔE 로 실측한 결과다.
(ΔE 20↑ 확실히 구분 / 11~20 주의 / 11↓ 혼동)

  계열 2개  : 색만으로 구분해도 안전하다.
              현행 파랑#3b82f6 + 빨강#ef4444 조합은 모든 색각 유형에서 ΔE 84 이상.
  계열 3개  : 아래 조합을 쓴다. 최악 ΔE 45.9 로 안전.
              어두운 무대 — 하늘#56b4e9 / 노랑#facc15 / 빨강#ef4444
              밝은 배경   — 파랑#1d4ed8 / 황갈#b45309 / 검정#1f2328  (ΔE 38.2)
  계열 4개↑ : ★ 색만으로는 불가능하다. 반드시 두 번째 채널을 함께 준다. ★
              (모양·패턴·직접 레이블·굵기 중 하나)
              실측 함정: 파랑#1d4ed8 과 보라#6d28d9 는 녹색약(D)에서 ΔE 0.4 —
              사실상 같은 색이다. 하늘#56b4e9 과 연두#34d399 는 청색약(T)에서 ΔE 5.6.
              "예뻐 보이는 4색 팔레트"는 거의 전부 이 함정에 걸린다.

두 번째 채널 예시:
  - 입자: 원 / 사각 / 삼각 (.legend .swatch, .swatch.sq, .swatch.tri)
  - 선  : 실선 / 파선 setLineDash([6,4]) / 점선 [2,3]
  - 원자: 원 안에 원소 기호를 직접 쓴다(가장 확실하다)

그 밖에:
  - 캔버스는 <canvas> 만으로 읽히지 않는다. 옆에 텍스트 측정값을 반드시 둔다.
    (측정값 카드가 곧 스크린리더 대체 텍스트 역할을 한다)
  - <canvas aria-label="..."> 로 무엇을 그리는 중인지 한 줄 설명한다.
  - 깜빡임: 1초에 3회 이상 번쩍이는 효과 금지(광과민성 발작 위험).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§10. 애니메이션·성능 — 교실 기기 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- requestAnimationFrame 루프 하나만 쓴다. setInterval 금지.
- 무거운 계산은 프레임당 상한을 둔다. 입자 충돌은 O(N²)다.
  N=300이면 프레임당 약 45,000쌍 — 구형 태블릿에서 끊긴다.
  ★ 슬라이더 max 를 "가장 느린 학생 기기에서 60fps 가 나오는 값"으로 잡는다.
    공간 분할(격자) 최적화는 코드를 크게 복잡하게 만든다.
    성능 문제가 실제 기기에서 확인되기 전에는 도입하지 않는다.
- 탭이 안 보일 때는 루프를 멈춘다:
    document.addEventListener('visibilitychange', ...) 로 rAF 중단.
    안 그러면 학생 태블릿 배터리가 녹는다.
- 이징은 ease-out 계열, 지속 시간 120~240ms. 400ms 넘는 UI 전환은 느리게 느껴진다.
- prefers-reduced-motion 은 공통 CSS가 처리한다.
  단 캔버스 애니메이션은 CSS로 못 막으므로, JS에서도 확인해
  matchMedia('(prefers-reduced-motion:reduce)').matches 이면
  자동 재생 대신 일시정지 상태로 시작한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§11. 활동지·학생 데이터
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 활동지 질문은 5~8개. 관찰(무엇이 보이나) → 관계(무엇이 무엇에 비례하나)
  → 설명(왜 그런가) → 심화(모형의 한계는) 순으로 배열한다.
- 마지막 문항은 "이 모형이 실제와 다른 점"을 묻는다. 근사와 한계를 드러내는 것이
  시뮬레이션 수업의 핵심이다.
- 학생 제출: 1단계는 CSV 내려받기. 이름 대신 좌석번호·익명코드를 권한다.
  실명 저장은 학교 개인정보 처리 절차 대상이 될 수 있다.
- 서버 저장으로 올라갈 경우(Supabase 등) RLS 정책을 반드시 건다.
  안 걸면 학생이 개발자도구로 남의 답을 전부 읽거나 지울 수 있다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§12. CSS 작성 규칙 — 이 사이트에서 실제로 터진 함정
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① 페이지 <style> 은 shared/style.css 보다 뒤에 온다. 특이도가 같으면 페이지가 이긴다.
   → 공통 CSS의 터치 규칙에는 html 접두어가 붙어 있다(html input[type=text]{...}).
     이 접두어를 지우면 iOS 자동 확대 버그가 되살아난다. 건드리지 말 것.
   → 페이지에서는 요소 선택자(button{}, input{}) 대신 클래스를 쓴다.
② 색을 페이지에 직접 적지 않는다. var(--d-blue) 처럼 토큰으로 쓴다.
   토큰에 없는 색이 필요하면 그건 대개 "필요 없는 색"이다.
   정말 필요하면 shared/style.css 에 토큰으로 추가하고 대비비를 실측해 주석에 적는다.
③ 캔버스 안 색은 CSS 변수를 JS에서 읽어 쓴다:
   getComputedStyle(document.documentElement).getPropertyValue('--p-sky').trim()
   하드코딩하면 나중에 사이트 색을 바꿔도 캔버스만 옛 색으로 남는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
§13. 배포 전 검증 — 3단계를 구분해 보고한다
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
"구현됨"과 "검증됨"은 다르다. 아래 3단계를 섞어 적지 않는다.

① 정적 검증 — node --check, 링크·경로 대소문자 대조, 색 토큰 사용 여부
② 수치 검증 — 물리 계산을 Node 스크립트로 돌려 "아는 정답"과 대조
   (예: 보일 법칙이면 PV 가 일정한지, 적정이면 당량점 pH 가 이론값과 맞는지)
   ★ 화면을 만들기 전에 계산 함수부터 이 검증을 통과시킨다.
③ 렌더링 실측 — 실제 브라우저에서 해상도별로 그려 보고 측정
   744 / 820 / 1194 / 360px 에서 가로 스크롤·터치 타깃·글자 크기 확인

배포 전 자동 스캔 2종:
  (1) 금지 패턴  : fetch(  /  import   /  type="module"  /  localStorage
                  /  https://  (외부 CDN)  → 전부 0건이어야 한다
  (2) 하드코딩 색: 페이지 파일에서 #[0-9a-f]{6} 검색 →
                  각 건이 토큰으로 대체 가능한지 개별 확인
```

---

# 부록 A — v3.0 → v4.0 대응표

| 항목 | v3.0 (단일 파일) | v4.0 (웹사이트) |
|---|---|---|
| **토큰 선언** | 파일마다 `:root` 새로 선언 | **`shared/style.css` 것만 사용, 재선언 금지** |
| **레이아웃** | 100vh 앱 셸 (TopBar·사이드·StatusBar) | **문서형 세로 스크롤**, 100vh 금지 |
| **상단 메뉴** | 직접 그림 | `shared/nav.js` 자동 삽입 |
| **페이지 배경** | `#e9e7e2` 웜그레이 | `#f5f7fa` (사이트 기존값 계승) |
| **관찰 영역** | 항상 순백 1종 | **어두운/밝은 2종 + 선택 규칙(§5)** |
| **데이터 컬러** | 인쇄용 원색 7종 | 동일 철학, **웹 배경 기준으로 재측정한 값** |
| **색각 대응** | "패턴 병용" 원칙 서술 | **색 개수별 규칙 + ΔE 실측 근거(§9)** |
| **터치 대응** | 언급 없음 | 44px 규범 + 특이도 함정 명시(§7·§12) |
| **성능** | 언급 없음 | **O(N²) 상한·visibilitychange·dpr 상한(§10)** |
| **검증** | 형광 잔재 스캔 2종 | **3단계 구분 보고 + 금지 패턴 스캔(§13)** |
| 색 철학 · CPK · 광량 위계 · 물리 정확성 | — | **그대로 계승** |

# 부록 B — 이 문서의 수치는 어디서 왔나

- **대비비**: WCAG 2.1 상대휘도 공식으로 직접 계산. 흰 카드(`#ffffff`)와
  페이지 배경(`#f5f7fa`) 양쪽 기준으로 측정해 낮은 쪽을 기재했다.
- **색각 ΔE**: Viénot–Brettel–Mollon(1999) 선형 근사로 적색약·녹색약·청색약을
  시뮬레이션한 뒤 CIELAB ΔE76 으로 모든 색 쌍의 거리를 계산, 최악값을 기재했다.
- **성능 수치**(N=300 → 45,000쌍): `gas/sim.js` 의 충돌 판정 구조에서 산출.
- ⚠ **아직 실측하지 않은 것**: 이 매뉴얼의 값들이 실제 학생 태블릿·빔프로젝터에서
  어떻게 보이는지는 확인하지 않았다. 첫 수업 후 실제 관찰 결과로 갱신할 것.

# 부록 C — 참고한 외부 설계 원칙

- **PhET Interactive Simulations** — 암묵적 안내(implicit scaffolding).
  설명문 없이 인터페이스의 제약과 행동 유도성만으로 학생을 교육적으로 유의미한
  조작으로 이끈다. 125개 시뮬레이션·600여 회 학생 인터뷰로 다듬어진 프레임워크.
  → §2-④, §3의 "기본값이 이미 볼 만한 상태" 규칙
- **Mayer, 멀티미디어 학습 원리** — 신호(signaling)·분절(segmenting)·
  공간 근접(spatial contiguity)·일관성(coherence).
  → §2-⑤⑥, §3의 배치 순서와 범례 규칙
- **WCAG 2.2 SC 2.5.8 Target Size** — 최소 24×24 CSS px.
  이 사이트는 학생 태블릿 환경이라 더 엄격한 44px 를 적용. → §7
- **Okabe–Ito 색각 안전 팔레트** — 명도·채도가 서로 다른 색을 고르고
  혼동이 심한 황록 구간을 피한다는 설계 사상. 팔레트를 그대로 쓰지는 않았고
  (흰 배경 대비가 부족한 색이 있다) 사상만 가져와 자체 측정으로 재구성했다. → §9

---

*v4.0 · 2026-07-28 · 화학 탐구실 저장소 내부 문서*
*v3.0(단일 파일 · 라이트)은 폐기되지 않는다. 사이트 밖에서 혼자 도는 파일을 만들 때는 v3.0을 쓴다.*
