# 화학 탐구실 — 시뮬레이션 작업 매뉴얼 v1.1

> **이 문서 하나로 새 시뮬레이션을 처음부터 끝까지 만들 수 있다.**
> 무엇을 만들지만 정하면, 순서·규범·검증·함정이 전부 여기 있다.
>
> v1.1 갱신 2026-07-31 · 대상 저장소 `화학탐구실/` · 작성 모델 `claude-opus-5`
> v1.0 → v1.1: 4부에 함정 ⑩~⑯(세션 5 실측) 추가 · §9 ΔE 인쇄 오류 정정 · §3·§4·§7·§11 분기 신설
> v1.1 추가반영 2026-08-04(세션 6 실측): 4부에 함정 ⑰~⑳ 추가 · 1부 P1에 「구조 위상 검증」·「겹침 검사」 행 신설 · 3부에 `topology_check.js`(겹침 검사 포함) 추가 · 부록 B 갱신. **파일명은 그대로 v1.1을 유지한다(신규 파일 생성 금지 — 사용자 지시).**
> v1.1 추가반영 2026-08-06(세션 8): 2부 §0(시뮬레이션 유형 — 이해형/체험형) 신설 · §1-3(체험형 3D 에셋 선행 제작 파이프라인)·§1-4(렌더링 기본값 표준) 신설 · §2·§10·0부 교차참조. 작성 모델 `claude-fable-5`. **파일명 v1.1 유지.**
> v1.1 추가반영 2026-08-13(세션 10): §1 페이지 하단 고정 스크립트 3줄→4줄(QR 버튼) · §7에 「■ QR 코드 버튼」 규범 신설 · 부록 A·5부 프롬프트 블록 반영. 작성 모델 `claude-sonnet-5`. **파일명 v1.1 유지.**
> v1.1 추가반영 2026-08-15(세션 11): `liquid/` 4단계 점진 공개 개편(단계 탭·정답 게이팅·나란히 비교·화학 표현 오류 수정) 실측 반영 — 1부 P5에 「화학 표현 5대 금지(J-N1~J-N5)」 신설 · 2부 §13(단계식 공개·정답 게이팅) 신설 · 3부 갱신 · 4부 함정 ㉑~㉖ 추가 · 부록 A·B 갱신. 작성 모델 `claude-fable-5`. **파일명 v1.1 유지.**
> v1.1 추가반영 2026-08-17(세션 12): 사용자가 `press`·`geotime-ride`·`mineral`·`liquid` 네 시뮬레이션의 카탈로그 제목을 직접 개정(활동 서술 제거 → 주제어 또는 「주제+개념」)한 것을 일반 규칙으로 승격 — **2부 §0-1(제목 짓기)** 신설. 작성 모델 `claude-sonnet-5`. **파일명 v1.1 유지.**
> v1.1 추가반영 2026-08-24(liquid 5탭 개편 세션 — 닫힌 용기 동적 평형 신설·거시↔분자 전환·atm 병기): **2부 §14(입자 모형 — 거시↔분자 전환·렌더러 이식·동적 평형 표현)** 신설 · 4부 함정 ㉗~㉙ 추가 · 3부 `liquid_check` 항목 수 갱신(75→105) · 부록 A 참고 구현 갱신 (사용자가 결과를 승인하고 매뉴얼 반영을 지시함). 작성 모델 `claude-fable-5`. **파일명 v1.1 유지.**

> **v1.1a (2026-08-27 · redox 세션 환류)** — 4부에 함정 **㉛~㊱ 6건** 추가(화면 픽셀 측정 3종
> 오류 · 외부 조달 three.js 자산의 통합 보정 3종 · reduced-motion 시간 상수화 · 게이트가 점수가
> 오르는 방향으로 뚫림 · 자기검사 대조군 오염 · 「고쳤다」 허위 보고와 검토 중 판 변경).
> 3부에 `검토.js` 유의사항 2건(첫 GL 컨텍스트 셰이더 경고 판별법 · WebGL 캔버스 미측정),
> §1-3 ④에 「우리 무대 배경으로 검증」, 0부에 「검사 없는 경로의 침묵」.

## 이 문서가 흡수한 것

| 흡수한 문서 | 어디로 갔나 |
|---|---|
| `디자인매뉴얼_v4.0.md` (2026-07-28) | **2부 전체** — 내용을 그대로 옮기고 실측으로 바로잡은 곳만 표시했다 |
| `검토파이프라인_v1.md` (2026-07-31, 저장소 바깥) | **1부 · 3부** |
| 세션 1~4에서 실제로 터진 함정 기록 | **4부 ①~⑨** — 이 사이트에서 진짜로 벌어진 일만 적었다 |
| 세션 5 — `liquid/` 개선 + 전수 재점검 (설계지시안_액체끓음시뮬개선_v3.1 · 재점검보고서_사이트전체_v1, 둘 다 저장소 바깥) | **4부 ⑩~⑯ · §3·§4·§7·§9·§11 갱신** |
| 세션 6 — `mineral/` 6종→9종 확장 + 연속 줌·비교 모드 신설 (설계지시안_고체시뮬레이션개선_v2, 저장소 바깥) | **4부 ⑰~⑳ · 1부 P1 · 3부 갱신** |
| 세션 8 — 체험형 3D 분기 + 에셋 선행 제작 파이프라인 (geotime-ride 렌더 개선 근거) | **2부 §0·§1-3·§1-4 신설 · §2·§10·0부 교차참조** |
| 세션 11 — `liquid/` 4단계 점진 공개 개편 (설계지시안_액체끓음시뮬단계화_v1.4 · 실행BC_v1.1 · 검증보고 2종, 전부 저장소 바깥) | **1부 P5 J-N 5칙 · 2부 §13 · 3부 갱신 · 4부 ㉑~㉖** |

→ **앞으로는 이 문서만 보면 된다.** `디자인매뉴얼_v4.0.md` 는 이력 보존용으로 남겨 두되,
   내용이 다르면 **이 문서가 이긴다.**

> ⚠ **이 폴더의 내용물은 그대로 GitHub 공개 저장소 루트다.** 이 문서도 웹에 공개된다.
> 학생 개인정보·비공개 자료를 이 문서에 적지 말 것.
> (공개해도 되는지 아직 확정되지 않았다면 인수인계서의 「공개 범위」 항목을 확인할 것)

---

# 0부 · 30초 요약

새 시뮬레이션을 만들 때 이 순서를 지킨다. 건너뛰면 나중에 두 배로 든다.

```
① 무엇을 깰 것인가 정한다      → 로드맵에서 ★ 오개념을 찾는다      (1부 P0)
② 계산 함수만 먼저 만든다       → Node 로 "아는 정답"과 대조        (1부 P1)
③ 화면을 만든다                → 2부의 규범을 따른다
④ 문법·규약을 훑는다           → 금지 패턴 0건                     (1부 P2)
⑤ 실제 브라우저에서 잰다        → 4해상도 × 전 탭                   (1부 P3)
⑥ 스크린샷을 눈으로 본다        → ★ 이걸 빼면 절반만 한 것이다       (1부 P4)
⑦ 오개념·색을 점검한다          → 표를 채운다                      (1부 P5·P6)
⑧ 교사에게 넘긴다              → 화학적·교육적 판단은 AI가 못 한다   (1부 P7)
```

> ※ 체험형(3D 라이드·모의실험)이면 ②와 ③ 사이에 «에셋 파이프라인»(§1-3)이 들어간다 — 착수 전 에셋을 파악·승인·검증한다.

**가장 자주 하는 실수 세 가지** (전부 이 사이트에서 실제로 일어났다)

1. 화면부터 만들고 나중에 수치를 맞추려 한다 → ②를 먼저 하면 30분이면 끝날 일이 반나절이 된다.
2. 자동 검사가 통과했으니 됐다고 생각한다 → ⑥에서 잡힌 결함이 지금까지 **13건**이다(세션 11에서 6건 추가).
   그리고 **검사가 «없는» 경로의 결함은 통과가 아니라 침묵이다** — redox의 A급 결함은
   reduced-motion 경로에 검사가 없어 두 검토 라운드를 살아남았다(4부 ㉝).
3. 잘 돌아가는 것을 이유 없이 손댄다 → **측정 없는 개선은 개선이 아니다.**

---

# 1부 · 작업 절차 (검토 파이프라인 P0~P7)

각 단계에 **통과 기준**과 **누가 판정하는가**가 붙어 있다. 앞 단계를 통과하지 못하면 다음으로 가지 않는다.

```
  P0 설계 게이트   ─ 만들기 전에 통과해야 하는 것
  P1 수치 검증     ─ 화면 없이 계산 함수만
  P2 정적 검증     ─ 문법·규약·금지 패턴
  P3 렌더링 실측   ─ 실제 브라우저 측정
  P4 시각적 직관성 ─ 학생이 3초 안에 읽는가        ★자동 3 + 육안 4
  P5 오개념 유발   ─ 화면이 잘못 가르치는가
  P6 색·모형 적절성 ─ 색각·CPK·스케일
  P7 교육적 타당성 ─ 교사만 판정 가능 (AI 판정 금지)
```

## P0. 설계 게이트 — 코드를 한 줄도 쓰기 전에

**판정: AI가 작성 → 사용자(교사)가 승인**

아래 5칸을 채우지 못하면 착수하지 않는다.

| 칸 | 내용 | 통과 기준 |
|---|---|---|
| **① 대응 오개념** | 로드맵에서 인용. 등급(★~★★★)과 차시 번호를 그대로 | 최소 1개, 로드맵 원문 인용 |
| **② 반박 문장** | "이 시뮬레이션은 학생의 **『…』**라는 생각을 화면으로 반박한다" | 한 문장. 추상적이면 실패 |
| **③ 반박 장치** | ②를 무엇으로 보여 주는가 (토글·카운터·2색 도표…) | 학생이 **직접 조작**하는 것. 설명 문구는 장치가 아니다 |
| **④ 모델의 가정** | 무시한 항 / 근사 / 임의 단위 / 과장한 스케일 | 각 항목이 활동지 어디에 드러나는지 표시 |
| **⑤ 변인 표** | 조작 / 고정 / 관찰 변인 | 조작 변인이 3개를 넘으면 탭으로 쪼갠다 |

> ★ **③이 이 사이트 시뮬레이션의 존재 이유다.** ③ 없이 "예쁜 데모"를 만들면 목적이 사라진다.

**★★ P0 에서 반드시 확인할 것 — 로드맵이 이 도구를 어디에 쓰라고 했는가**

로드맵은 도구마다 **목적을 한정**해 두었다. 이걸 어기면 물리를 아무리 정확히 짜도 틀린 시뮬레이션이 된다.
세션 4에서 실제로 이것 때문에 엔진을 통째로 다시 썼다(4부 ①).

> 로드맵 Ⅰ단원 §4: "PhET Gas Properties는 **이상 기체 모형 구현체**다. 이것으로 실제 기체의
> 비이상성을 관찰시킬 수는 없다. 4차시에서는 '이 모형에서는 절대 일어나지 않는 일 찾기'로
> 목적을 한정한다. **비이상성은 PV/nRT 실측 데이터 표로 다룬다.**"
>
> 로드맵 Ⅰ단원 §4: "**게임은 개념 형성 차시(2·4·8·10·11)에 넣지 않는다.** 숙달(9·12)과 논증(15)에만."

## P1. 수치 검증 — 화면을 만들기 **전에**

**판정: `검증스크립트/<이름>_check.js` (자동)**

| 항목 | 통과 기준 |
|---|---|
| 아는 정답 대조 | **최소 5항목.** 교과서 값·문헌값·해석해와 대조 |
| 오차 명시 | 1 % 초과 항목은 **왜 다른지** 주석에 이유를 적는다. 이유를 못 적으면 버그 |
| 경계값 | 모든 슬라이더 min·max, 0, 극단 조합에서 `NaN`·`Infinity`·음수 없음 |
| 단조성 | 물리적으로 단조여야 하는 관계가 실제로 단조인가 |
| **라이브 수렴성** | ★ 슬라이더를 만진 뒤 **몇 초 안에 안정되는가**, 평형에서 **얼마나 흔들리는가** |
| **구조 위상 검증** (v1.1 세션 6 신설) | 격자·결정 구조를 그리는 시뮬레이션은 **결합선이 실제로 옳은 원자쌍만 잇는지**, 고립점 비율, 연결 덩어리(그물) 비율을 종마다 자동 검사한다. 좌표·수치가 맞아도 연결 관계가 틀릴 수 있다 |
| **겹침(overlap) 검사** (v1.1 세션 6 신설) | 3D 배치 좌표는 모든 원자쌍의 거리를 반지름 합과 비교해 **겹치는 쌍 0건**을 확인한다. 밀도가 맞아도 개별 좌표가 겹칠 수 있다 |

> ★ **마지막 항목이 없어서 세션 4에서 결함을 놓칠 뻔했다.** 평균만 재면 진동을 못 잡는다.
> 반드시 "시간에 따른 흔들림"을 재라. (4부 ②)
> ★ **위상·겹침 검사가 없어서 세션 6에서 결함을 놓칠 뻔했다.** 값과 스크린샷만으로는
> "몇 번째 원자와 몇 번째 원자가 이어졌는가", "좌표가 서로 겹치는가"를 판정할 수 없다. (4부 ⑰⑱)

**"아는 정답"의 좋은 예 — 이 사이트에서 실제로 쓴 것**

| 시뮬레이션 | 대조한 정답 |
|---|---|
| `gaslaws` | 샤를 외삽선의 x절편 = 0 K = **정확히 −273 ℃** / PV 흩어짐 0.000 % |
| `press` | 1 mol · 273.15 K · 22.414 L → **101.3 kPa** (= 1 atm) / 1 kPa × 100 cm² = **10 N** |
| `liquid` | Antoine 상수로 계산한 기준 끓는점이 문헌과 **0.2 ℃ 이내** / 곡선 기울기 = 기화 엔탈피 |
| `mineral` | **격자 상수로 밀도를 역산**해 문헌 밀도와 대조 (세션 6 기준 9종 중 결정성 8종 모두 **0.3 % 이내**. 단일 문헌 밀도점이 없어 온도 의존이 큰 종은 별도 판정 기준을 세우고 그 사유를 스크립트 주석에 적는다) |

마지막 것이 특히 강력하다. 격자 상수가 맞으면 밀도가 저절로 맞는다 — 서로 독립인 두 값이므로.

## P2. 정적 검증

**판정: 셸 (자동)**

| 항목 | 통과 기준 |
|---|---|
| `node --check` | 전 파일 통과 |
| 금지 패턴 | `fetch(` · `type="module"` · `import ` · `localStorage` · `sessionStorage` · `http://` · `https://` → **0건** |
| `:root` 재선언 | 페이지 `<style>` 안에 **0건** |
| 하단 3줄 규약 | `../sims.js` → `../shared/nav.js` → `sim.js` 순서 일치 |
| `sims.js` 정합성 | id 중복 0 · `ready:true` 인데 파일 없음 0 · 아이콘 누락 0 |
| **기존 항목 보존** | `sims.js` 수정 후 기존 항목·아이콘 **손실 0건** (파싱 대조) |

```bash
S="<세션경로>/mnt/[AI] 시뮬레이션 사이트/화학탐구실"; cd "$S"
for f in sims.js shared/nav.js */sim.js; do node --check "$f"; done
for p in 'fetch(' 'type="module"' 'localStorage' 'https://'; do
  printf "%-16s %s\n" "$p" "$(grep -rlF -- "$p" gaslaws press liquid mineral 2>/dev/null | wc -l)"; done
node -e 'const fs=require("fs");eval(fs.readFileSync("sims.js","utf8")+";global.S=SIMS;global.I=ICONS;");
  let b=0; for(const s of S){ if(s.ready&&!fs.existsSync(s.path+"index.html")){console.log("✗",s.path);b++}
  if(!I[s.icon]){console.log("✗ icon",s.id);b++} }
  console.log("전체 "+S.length+" / ready "+S.filter(s=>s.ready).length+" / 문제 "+b);'
```

## P3. 렌더링 실측

**판정: `검증스크립트/검토.js` (자동)**

| 항목 | 통과 기준 |
|---|---|
| 해상도 | 360 / 744 / 820 / 1194 px, `hasTouch:true`, `deviceScaleFactor:2` |
| 조작 범위 | **모든 탭 클릭 + 모든 슬라이더 min·max + 모든 체크박스 토글** 후 재측정 |
| 가로 스크롤 | 0 px (조작 전·후 **둘 다**) |
| 터치 타깃 | 44 px 이상. **체크박스는 감싼 `<label>`의 크기로 잰다** |
| 입력창 글자 | 16 px 이상 (iOS 자동 확대 방지) |
| 캔버스 | 픽셀 알파 샘플링 → 빈 캔버스 0개 |
| 콘솔 | 오류·미처리 예외 0건 |

## P4. 시각적 직관성

### P4-A. 자동 3항목 — `검토.js` 가 측정

| # | 항목 | 방법 | 통과 기준 |
|---|---|---|---|
| A1 | **변화 감지** | 슬라이더를 min·max 로 밀고 캔버스를 픽셀 차분 | 변한 비율 **≥ 2 %**. 미만이면 "조작해도 화면이 안 바뀐다" |
| A2 | **가장자리 잘림** | 캔버스 가장자리 3 px 띠에 데이터 색이 닿았는지 | 닿으면 ✗ (배경·격자는 제외) |
| A3 | **글자 겹침** | 캔버스 위 오버레이 사각형 아래에 캔버스 내용이 있는지 | 겹침 0 |

### P4-B. 육안 4항목 — 스크린샷을 **반드시 눈으로 본다**

| # | 질문 |
|---|---|
| B1 | **3초 규칙** — 3초 봤을 때 "무엇이 주인공인지" 하나로 지목되는가 |
| B2 | **인과 근접** — 조작한 슬라이더와 결과 수치가 **스크롤 없이 한 화면**에 같이 보이는가 (1024px 미만 1단에서도) |
| B3 | **기본값** — 첫 화면이 이미 "볼 만한 상태"인가. 빈 무대·전부 0인 표로 시작하지 않는가 |
| B4 | **여백** — 무대 안이 한쪽으로 쏠려 허전하지 않은가 |

> ★ **이 단계를 건너뛰면 절반만 한 것이다.** 4부의 함정 7건 중 **5건**이 여기서만 잡혔다.
> 세션 11에서도 자동 검사가 전부 통과한 뒤 **시각 전용 결함 6건**(캡션 잘림·라벨 겹침 2건·
> 비활성 버튼이 활성처럼 보임·캔버스 높이 고정·빈 상태 문구 잘림)이 스크린샷 육안으로만 잡혔다.

## P5. 오개념 발생 가능성

**판정: AI가 목록 작성 → 각 항목에 대응 표기 → 교사가 최종 확인**

시뮬레이션은 **말한 것뿐 아니라 그린 것으로도 가르친다.**
아래 8개 **전 항목**에 "해당 없음" 또는 "이렇게 막았다"를 적는다.

| # | 위험 | 점검 질문 | 이 사이트의 대응 예 |
|---|---|---|---|
| M1 | **은유 오염** | 입자·물체의 **위치**가 잘못된 인과를 암시하지 않는가 | `vapor/` 는 용질을 수면에 깔지 않고 액체 전체에 흩뿌린다 |
| M2 | **목적론** | "…하려고 움직인다"로 읽히지 않는가 | `osmosis/` 는 양방향 카운터로 "농도를 맞추려고"를 차단 |
| M3 | **정지 = 평형** | 평형을 정지 화면으로 그리지 않았는가 | 두 방향 화살표를 **같은 굵기**로, 애니메이션은 계속 돌린다 |
| M4 | **스케일 왜곡** | 크기·시간·압력 환산이 실제와 몇 배 다른가 | `press/` 는 피스톤 폭을 A<sup>0.55</sup> 로 줄여 그리고 **정확한 비를 숫자로** 따로 보인다 |
| M5 | **이상화 은폐** | 이 모형에서 **절대 일어나지 않는 일**을 학생이 알 수 있는가 | `gaslaws/` 활동지 8번 ⓐ "액화가 일어나지 않는다. 왜?" |
| M6 | **색의 오해** | 색이 화학적 의미를 잘못 부여하지 않는가 | 원자는 CPK 고정. 임의 색이면 범례에 "임의 색"이라 적는다 |
| M7 | **정밀도 과장** | 유효숫자가 모형의 정밀도보다 많지 않은가 | 임의 단위는 `a.u.` 표기 + 활동지에서 1회 설명 |
| M8 | **범위 = 세계** | 슬라이더 범위 밖이 "존재하지 않는 값"으로 읽히지 않는가 | 범위 끝에 "여기까지만 조작 가능(교육적 제한)"을 표시 |

### P5 단원 전용 — Ⅰ 물질의 세 가지 상태

각 시뮬레이션은 자기가 담당하는 것을 **반드시 반박**하고, 담당하지 않는 것도 **새로 심지 않았는지** 확인한다.

| 등급 | 오개념 | 담당 | "새로 심지 않았는가" 점검 |
|---|---|---|---|
| ★★★ | 온도·압력이 변하면 **분자 자체의 부피**가 변한다 | `gaslaws/` | 압축 애니메이션에서 입자 크기가 **화면상 절대 변하지 않아야** 한다 |
| ★★★ | **실제 기체 = 이상 기체** | `gaslaws/` `press/` | 이상 기체 모형임을 화면에 명시. 액화가 없음을 발문으로 |
| ★★★ | **끓는점 = 분자 내 결합 파괴** | `liquid/` | 끓는 그림에서 분자가 **원자로 쪼개지면 안 된다** |
| ★★ | 온도·압력이 변하면 **분자 수 n**이 변한다 | `gaslaws/` | 입자 카운터를 **상시 노출** |
| ★★ | −273 ℃에서 **실제로** 부피가 0 | `gaslaws/` | 실측 구간과 외삽 구간을 **다른 색·점선**으로 |
| ★★ | 증기 압력은 액체의 **양·용기 부피**에 따라 달라진다 | `liquid/` | 액체량 슬라이더를 넣고 증기압이 **안 변함**을 보인다 |
| ★★ | **같은 온도**에서 액체들의 증기 압력이 같다 | `liquid/` | 같은 온도 비교와 각자의 끓는점 비교를 **둘 다** 제시 |
| ★ | 결정 = 예쁜 모양 / **유리는 흐르는 액체** | `mineral/` | 불투명한 결정(황철석·구리)을 반드시 포함 |
| ★ | **이온 결정은 고체에서 전기가 통한다** | `mineral/` | 결합 종류 표시에 고체·융해액 전도성을 함께 |

### P5 화학 표현 5대 금지 — J-N1~J-N5 (세션 11 승격)

`liquid/` 개편에서 외부 검토·S-검토가 실제로 잡은 화학 서술 오류를 일반 규칙으로 승격한 것.
적용 범위는 **학생에게 닿는 모든 문자열** — 화면 본문·범례·aria-label·CSV 헤더·파일명까지(세션 11 사용자 확정).

| # | 금지 | 실측 전례 (`liquid/`) |
|---|---|---|
| J-N1 | 분자간 힘을 배타적으로 분류하지 않는다("분산력**만**" 류) | 극성 분자인 에터에 "분산력만"이라 적어, 같은 카드의 극성 표기와 자기모순 |
| J-N2 | 기화 에너지·결합 해리 에너지·전기분해 에너지를 섞지 않는다. 에너지 값에는 **상태(l/g)를 병기**한다 | "분자 분해 ≈ 486 kJ"이 결합 해리와 전기분해를 뒤섞었다. 상 구분 생략 금지는 사용자 확정 |
| J-N3 | 열린 계의 계산값을 그냥 "증기 압력"이라 부르지 않는다 — **"포화 증기 압력"** + 맥락 병기 | "지금 증기 압력"이 포화 증기 압력 계산값을 오표기 |
| J-N4 | 끓음에서 분자간 힘은 **극복**되는 것이지 **끊어지는** 것이 아니다 | "끓는점 = 결합 파괴" ★★★ 오개념과 직결 — P5 단원 표의 `liquid/` 담당 항목 |
| J-N5 | 화면 문구는 **같은 화면에 보이는 수치와 모순되면 안 된다** | 4부 ㉒ — 배속 안내가 가장 느리게 끓는 물에서 "매우 빨리 끓습니다"를 띄웠다 |

## P6. 색 배치·모형 적절성

| # | 항목 | 통과 기준 |
|---|---|---|
| C1 | 토큰 사용 | 캔버스 색을 `getComputedStyle(...).getPropertyValue('--p-sky')` 로 읽어 쓴다 |
| C2 | 무대 모드 일치 | 어두운 무대에는 `--p-*`, 밝은 무대에는 `--d-*`. **섞지 않는다** |
| C3 | **색 개수 규칙** | 2색 = 색만으로 OK / 3색 = 2부 §9 지정 조합 / **4색 이상 = 두 번째 채널 필수** |
| C4 | **색각 ΔE 실측** | 데이터 색 전 쌍에 적·녹·청색약 시뮬레이션 후 ΔE76 → **최악 ≥ 20** |
| C5 | CPK | 원자색은 국제 표준 고정. `stroke` = fill 을 40~60 % 어둡게, 알파 0.85 |
| C6 | 대비 | 무대 배경 대비 데이터 색 **≥ 4.5:1** |
| C7 | 모형의 적절성 | 크기 비·시간 배율이 **실제와 몇 배 다른지 적혀 있는가** (M4 와 연동) |

**토큰으로 바꾸면 안 되는 색 두 가지** — `검토.js` 가 "하드코딩 색"으로 보고하지만 정상이다.

1. **CPK 원자색** — 국제 표준. O `#FF0D0D` · H `#FFFFFF` · C `#404040` · Na `#AB5CF2` · Cl `#1FF01F` 등
2. **물질의 실제 겉보기 색** — 액체 4종·광물 6종의 색. 사이트 테마 색이 아니다.

## P7. 교육적 타당성 — **AI 가 판정하지 않는다**

**판정: 사용자(화학 교사)**

AI 는 아래를 **질문 목록으로 만들어 넘기기만** 한다. "검증했다"고 쓰지 않는다.

- [ ] 활동지 문항이 해당 차시 성취기준에 부합하는가
- [ ] 난이도가 이 집단('화학' 이수자)에 맞는가
- [ ] 화면의 화학적 서술에 오류가 없는가
- [ ] 의도된 과장·근사가 수업에서 오개념을 만들지 않는가
- [ ] 성취기준 범위 밖 내용이 "외워야 하는 것"으로 오해되지 않게 표시돼 있는가
- [ ] 게임 요소가 개념 형성 차시를 방해하지 않는가

## 보고 서식 — 이렇게만 적는다

**통과하지 못한 항목은 "미검증"이라고 쓴다. "아마 괜찮을 것"은 쓰지 않는다.**

```
| 단계 | 결과 | 비고 |
|---|---|---|
| P0 설계 게이트 | 통과 / 사용자 승인 대기 | |
| P1 수치 검증  | n/n 통과 | 오차 1 % 초과 m건 — 이유 기재 |
| P2 정적 검증  | 통과 | |
| P3 렌더링 실측 | 16/16 통과 | |
| P4 직관성     | 자동 3/3 · 육안 4/4 | |
| P5 오개념     | M1~M8 전 항목 기재 · 단원 항목 k개 담당 | |
| P6 색·모형    | ΔE 최악 xx.x | |
| P7 교육 타당성 | **사용자 검토 대기** | AI 판정 없음 |
```

---

# 2부 · 설계 규범 (구 디자인매뉴얼 v4.0)

## §0. 시뮬레이션 유형 — 먼저 어느 쪽인지 정한다 (세션 8 신설)

**이 사이트의 시뮬레이션은 두 종류다. 무엇을 만들지 정할 때 어느 쪽인지 먼저 못 박는다. 이걸 안 나누면 2부·§10의 원칙이 엉뚱한 곳에 적용된다.**

| | **이해형(개념형)** | **체험형(실감형)** |
|---|---|---|
| 예 | gaslaws · liquid · press · mineral | geotime-ride(3D 라이드) · 모의실험 · 가상 야외조사 |
| 학생이 하는 일 | 변인을 조작해 **관계·오개념을 깬다** | 현장에 **들어가 관찰·경험한다** |
| 학습 기제 | 인지(추론·비교) | 현존감(presence)·정서적 몰입 → 상황적 흥미 |
| **충실도(fidelity)의 위상** | **장식** — 사실적 렌더는 주의를 분산시켜 이해를 방해 | **내용** — 관찰 대상의 사실성이 곧 학습 자료 |
| 지배 원칙 | §2-①②(광량 위계·데이터 중심·장식 금지) 그대로 | §2-③(물리적 정확성)은 유지, §2-②의 「장식 금지」는 **완화** |

> ★ **핵심 판정 한 줄**: 「시각적 사실성이 이 시뮬의 *내용*인가, *장식*인가.」
> - 내용이면(체험형) 조잡한 모델은 「검소한 디자인」이 아니라 **기능 결함**이다. 어설픈 공룡은 틀린 교과서 삽화와 같고, 깨진 몰입은 체험형의 교육 기제 자체를 무력화한다.
> - 장식이면(이해형) 사실적 렌더는 §2-②(데이터 중심) 위반이다. 기존 매뉴얼이 맞다.

**두 유형 공통으로 지키는 것 — 체험형이라고 예외가 아니다**

1. **하이브리드가 정답이다.** 체험형도 라벨·단면·수치·이름표 같은 **도식 오버레이**를 얹는다. 「사실적 세계 + 도식적 정보층」. geotime-ride의 클릭 정보 카드·관찰 포인트 3배지가 이 오버레이다(인수인계 §3-6). 사실성은 세계에, 판독성은 오버레이에 둔다.
2. **충실도는 §10 성능 예산 안에서만 추구한다.** 교실 PC에서 끊기는 고화질보다 **60fps로 도는 적당한 화질**이 체험 품질이 높다. 실기기 확인 전에는 화질을 올리지 않는다(§10 「측정 없는 개선 금지」 승계).
3. **정직성(§2-③·§11)은 유형 무관.** 사실적으로 보이게 만든 것과 크기·시간을 과장한 것은 **활동지/정보 카드에 명시**한다. geotime-ride의 「추정입니다」 카드·크기 과장 고지가 전례.

> ⚠ **§1-2(순수 WebGL·라이브러리 금지)의 예외는 체험형이다.** geotime-ride는 **로컬에 벤더링한 three.js r147**(`vendor/three.min.js`)을 쓴다. §1의 CDN 금지에 걸리지 않는다(외부에서 받아오지 않고 저장소에 포함). 체험형 3D는 순수 WebGL로 조명·스키닝·후처리를 직접 짜는 비용이 크므로 **벤더링된 three.js 사용을 허용**한다. 단 버전을 올리지 말고(r147 고정), 새 예제 유틸이 필요하면 인수인계 §5-1 방식으로 UMD 판을 `vendor/`에 넣는다.

## §0-1. 제목 짓기 — 카드 한 줄에 들어가게 (세션 12 신설)

`sims.js`의 `title`, 해당 페이지의 `<title>` 태그, 페이지 본문의 `<h1>` — 이 셋은 **하나의 값**이다(F-1, 단일 원천). 이 값은 대문 카탈로그 카드 한 줄(「이용 가능」 배지와 나란히 놓인다)에 들어가야 읽힌다. 길어서 두 줄로 꺾이면 배지와 겹치거나 잘린다.

**긴 제목이 생기는 원인은 하나뿐이다 — 활동 형식·설명을 제목에 욱여넣는다.** "1인칭 라이드", "물체를 부수지 않고 가장 세게 누르기", "겉모습에서 입자 배열까지", "끓음 실험" 은 전부 **무엇을 조작·관찰하는지에 대한 설명이지 이름이 아니다.** 그 설명은 카드의 `desc`와 페이지의 `.sub`(또는 `desc`) 문단이 이미 하고 있다 — **제목을 줄여도 그 문단은 그대로 둔다.**

**규칙**: 제목 = 주제어, 또는 「주제어 + 와/과 + 이 시뮬이 실제로 다루는 핵심 개념」. 활동 형식·긴 설명은 붙이지 않는다. 예외적으로 대결·도전형 게임은 성격을 알리는 「~게임」 한 단어만 허용한다.

| 유형 | 예 (2026-08-17 실개정) | 적용한 원칙 |
|---|---|---|
| 대결·도전 게임 | `press`: ~~"유압 프레스 도전 — 물체를 부수지 않고 가장 세게 누르기"~~ → **"유압 프레스 도전 게임"** | 활동 서술 제거 + 「게임」 한 단어로 성격만 표시 |
| 체험형 3D | `geotime-ride`: ~~"지질 시대의 바다 — 1인칭 라이드"~~ → **"지질 시대의 바다"** | 형식 서술("1인칭 라이드") 전부 제거 — 주제어만 |
| 이해형(연속 관찰형) | `mineral`: ~~"고체 — 겉모습에서 입자 배열까지"~~ → **"고체와 입자 배열"** | 주제 + 이 시뮬이 보여주는 핵심 개념을 「와/과」로 연결 |
| 이해형(조작 실험형) | `liquid`: ~~"액체 — 끓음 실험"~~ → **"액체와 끓는점"** | 주제 + 실제로 다루는 물리량(끓는점) — 활동명("실험")이 아니라 **대상 개념**을 쓴다 |

> ⚠ **"실험"·"조사"·"탐구"·"라이드" 같은 활동 명사를 제목에 쓰지 않는다.** 이 사이트의 시뮬레이션은 전부 실험·조사·탐구이므로 변별력이 없다. 대신 그 활동이 다루는 **개념 이름**(끓는점, 입자 배열, 압력…)을 쓴다.
> 이 표의 네 항목은 실제로 2026-08-17에 이 규칙에 맞춰 개정됐다 — 새 시뮬레이션 제목을 정할 때도, 기존 제목이 이 표의 왼쪽 열처럼 길어져 있는 걸 발견했을 때도 이 표를 기준으로 삼는다.

## §1. 기술 제약 — 타협 불가

- **프레임워크·빌드 도구·npm 금지.** 순수 HTML + CSS + JS + Canvas (+ 필요하면 WebGL).
- **`fetch()`·ES 모듈(`import`/`export`)·`type="module"` 금지.**
  → 쓰면 `file://` 에서 CORS 로 막혀 더블클릭으로 안 열린다. **취향이 아니라 요구사항이다.**
  → 스크립트는 반드시 일반 `<script src="..."></script>` 로만.
- **외부 CDN·웹폰트·이미지 파일 금지.** 아이콘은 인라인 SVG.
  → 학교 인터넷 필터링에 CDN 이 막히면 수업이 통째로 멈춘다.
- **`localStorage`·`sessionStorage` 금지.** 상태는 JS 변수로만.
- **폴더 구조**: 영문 소문자 폴더 하나에 `index.html` + `sim.js` 두 파일. 화면과 물리를 섞지 않는다.
- **페이지 맨 아래 4줄은 고정 형식** (v1.1 세션 10 — QR 버튼 추가로 3줄→4줄):
  ```html
  <script src="../sims.js"></script>
  <script src="../shared/nav.js" data-base=".." data-current="새id"></script>
  <script src="../shared/qr.js" data-base=".." data-current="새id"></script>
  <script src="sim.js"></script>
  ```
  `data-current` 값은 `nav.js`·`qr.js` 두 곳 다 같은 id 를 준다.
  ⚠ **전체화면(`100vh`) 구조라 `nav.js` 를 안 쓰는 페이지**(`universe/` 전례)는 `qr.js` 를
  자체 헤더 스크립트들 뒤, `</body>` 바로 앞에 한 줄만 추가한다 — 위치가 달라도 동작은 같다.
- **등록**: `../sims.js` 의 항목을 `ready:true` 로. 대문 카드·이동 메뉴·분류 칩이 자동 생성된다.

### §1-2. WebGL 을 쓸 때 (v1.0 신설)

3D 렌더링이 필요하면 **순수 WebGL 로 직접 쓴다.** three.js 등 라이브러리는 넣지 않는다(§1 CDN 금지).

- 전체 화면 사각형 하나 + 프래그먼트 셰이더가 기본형. 정점 데이터가 거의 없어 코드가 짧다.
- **반드시 폴백을 둔다**: `getContext("webgl")` 이 `null` 이면 안내문을 띄우고,
  **측정값·그래프·활동지는 그대로 쓸 수 있게** 한다. 학교 기기에서 가속이 꺼져 있을 수 있다.
- 셰이더 안 색도 가능하면 uniform 으로 넘긴다(캔버스 2D 의 토큰 규칙과 같은 취지).
- GLSL ES 1.0 에는 `transpose()` 가 **없다.** 회전 행렬의 역행렬은 직접 만들어 쓴다.
- 헤드리스 검증 시 `--enable-unsafe-swiftshader` 인자가 필요하다(`검토.js` 에 이미 들어 있다).

## §1-3. 체험형 3D 에셋 파이프라인 — 착수 «전»에 에셋을 파악한다 (세션 8 신설)

**첫 렌더가 실망스러운 구조적 원인**: 라이드 로직과 시각(유기체 지오메트리)을 한 코드에서 동시에 생성하면 유기체가 「프로그래머 아트」가 되고, 고치려면 통합 씬 코드 전체를 다시 만져 **토큰이 로직+시각 양쪽으로 이중 소모**된다. → 시각 요소를 **에셋으로 선분리**하면 시행착오가 에셋 단위 작은 반복으로 국소화되고, 완성된 `.glb`는 이후 세션에서 **토큰 0인 외부 자원**이 된다(코드에 박힌 절차적 지오메트리는 세션마다 다시 읽힌다 — `vendor/models.js` 16.8MB가 이미 그 역할).

**5단계. P0 설계 게이트를 통과한 «직후», 화면 코드를 짜기 «전»에 밟는다.**

| 단계 | 하는 일 | 통과 기준 / 산출물 |
|---|---|---|
| **① 에셋 목록(manifest)** | 씬을 분해해 필요한 3D 모델·텍스처·환경을 **표로 나열**. 각 항목에 용도·대략 개체 수·필요 충실도(주인공/배경)·애니메이션 필요 여부 | 표 하나. 「무엇이 화면에 나오는가」가 다 적혀야 함 |
| **② 사용자 승인** | ①을 교사에게 넘겨 **만들 것/뺄 것을 확정**받는다. P0 게이트와 같은 철학 | ★ **승인 없이 에셋을 만들지 않는다.** 안 쓸 에셋 선제작 = 토큰 손해 |
| **③ 조달** | 아래 **우선순위**대로 확보. 라이선스(CC-BY/CC0) 전수 확인·크레딧 기록 | 항목별 원본 파일 + 라이선스·출처 |
| **④ 에셋별 단독 검증** | **원칙 14(인수인계) 승계** — 통합 «전»에 헤드리스 뷰어(`viewer/`, 4×4 격자 이하)로 **한 개씩 눈으로** 본다. ⚠ **우리 무대의 배경색·색공간·조명으로** 렌더한다 — 발주처 데모 배경과 우리 무대가 다르면 그 차이만으로 이펙트가 사라진다(4부 ㉜) | 격자 렌더 스크린샷을 Read로 확인. 체커판 소품·법선 소실·엉뚱한 색·Z-up 눕힘·**혼합 모드/색공간 오류**를 여기서 잡는다 |
| **⑤ 라이브러리화 → 본 제작** | 검증 통과분만 `vendor/models.js`(또는 별도 에셋 파일)에 넣고, 본 제작은 **검증된 에셋을 로드**해 배치 + §1-4 렌더링 표준 적용 | `node --check` 통과 · 해당 `_check.js` 통과 후 배포 |

**③ 조달 우선순위 — 이 순서를 지킨다**

```
1순위  기존 CC 에셋 다운로드   Poly Haven · Quaternius · Kenney · CC-BY/CC0 Sketchfab
         → 사람이 만든 것이라 유기체 품질이 압도적. 토큰도 거의 안 든다.
2순위  절차적 생성             지형 · 실험 기구 · 결정격자처럼 «규칙적 형태»만
         → 유기체 말고 수학으로 기술되는 형태에만. (스트로마톨라이트 자체 제작 전례 인수인계 §3-7)
3순위  코드로 유기체 직접 조형  ★ 최후 수단. 토큰·품질 모두 최악의 조합. 피한다.
```

> ★ **유기체(생물·인물·자연물)를 three.js 프리미티브로 빚지 마라.** 세션 6이 절차적 도형에서 막혔던 것을 세션 7이 실사 모델 22종으로 교체해 품질이 대폭 올라간 것이 실측 증거다(인수인계 §3-3). 다운로드가 가능하면 무조건 다운로드다.

**제작 함정은 인수인계 §5-1을 승계한다(재기술하지 않음).** 요지만:
- **specGloss 확장**: Sketchfab 모델 다수가 `KHR_materials_pbrSpecularGlossiness` — 우리 `GLTFLoader.js`가 못 읽어 로드 실패. `gltf-transform`의 `metalRough()` 변환이 **필수**.
- **스캔 모델 감축**: NORMAL을 일부러 버려 `weld`가 붙게 한 뒤, 감축·prune «다음»에 법선 재생성(순서 중요).
- **Z-up 모델**(trimesh/blender): 크기 축 실측으로 판별해 빌드에 회전을 구워 넣는다.
- **`device_stage_files` 26MB+ 타임아웃**: 대용량 텍스처는 기기에서 1024px로 줄여 옮긴다.
- 파이프라인 도구: `@gltf-transform/core·extensions·functions` + `meshoptimizer` + `sharp`. **치환 스크립트는 파일로 써서 실행**(인라인 `node -e`는 조용히 실패).

> ⚠ **인수인계 §8.40 이월**: §5-1 파이프라인 스크립트(`pipeline.mjs`·`buildmodels.mjs`·`fixaxis.mjs`·`runstrom.mjs`)는 지금 샌드박스에만 있다. `models.js`가 그 산출물이라 스크립트 없이는 재생성이 안 된다. **`검증스크립트/`(저장소 밖)에 저장할 것.** 저장하면 이 파이프라인의 ③~⑤가 다음 세션에서 바로 재현된다.

## §1-4. 렌더링 기본값 표준 — 「모델은 좋은데 조잡해 보임」을 막는다 (세션 8 신설)

**첫 렌더가 나쁜 원인의 절반은 모델이 아니라 조명·재질·후처리다.** 좋은 모델도 기본 조명 아래선 플라스틱처럼 보인다. 체험형 3D는 씬을 세우기 전에 아래를 **기본값으로 깔고 시작**한다(three.js r147 기준).

| 항목 | 기본값 | 왜 |
|---|---|---|
| **톤매핑** | `ACESFilmicToneMapping` + `toneMappingExposure` 조정 | 기본 선형 출력은 하이라이트가 타고 색이 뜬다. ACES가 사진 같은 계조를 준다 |
| **출력 색공간** | sRGB 출력(r147 `outputEncoding = sRGBEncoding`), 텍스처도 sRGB로 로드 | 색공간 안 맞추면 전체가 어둡거나 바래 보인다 |
| **환경광(IBL)** | HDRI를 `PMREMGenerator`로 환경맵화, 최소한 `HemisphereLight`+`DirectionalLight` | 단일 방향광만 쓰면 그림자 쪽이 새까매져 유기체가 조잡해 보인다. **재질감의 8할이 환경광** |
| **그림자** | `PCFSoftShadowMap`, 주광원만 그림자 캐스팅 | 접지 그림자가 없으면 물체가 공중에 뜬 것처럼 보여 현존감이 깨진다 |
| **재질** | PBR(`MeshStandardMaterial`), metalness·roughness를 실물 기준으로 | 인수인계 §5-1 `metalRough` 변환과 짝. 사실성은 여기서 갈린다 |
| **깊이감** | `fog`(시대 색과 맞춘 색), 원경 페이드 | 원근·규모감을 만든다. geotime-ride 해저의 시대색 되물들임과 같은 취지(인수인계 §3-4) |
| **성능 상한** | dpr ≤ 2, WebGL 컨텍스트 ≤ 16개, `[화질:보통]` 폴백 | §10 예산 준수. 실기기 확인 전 화질 상향 금지 |

> ⚠ **§1-2 폴백 조항 승계**: `getContext("webgl")`이 null이면 안내문을 띄우고, **정보 카드·활동지는 그대로** 쓸 수 있게 한다. 학교 기기에서 가속이 꺼져 있을 수 있다.

> **검증**: 렌더링 표준 적용 전/후를 헤드리스 Chromium(`--use-angle=swiftshader --enable-unsafe-swiftshader`)으로 **before/after 비교 렌더**해 눈으로 확인한다(세션 7의 그래픽 검증 방식 승계). 「좋아 보인다」는 스크린샷으로 판정한다.

## §2. 설계 철학 6원칙

> ⚠ 아래 6원칙은 «이해형» 기준이다. 체험형(3D 라이드·모의실험)은 §0의 분기를 먼저 보라 — 특히 ②(장식 금지)는 완화된다.

**① 광량 위계** — 시선이 가장 먼저 닿아야 하는 곳은 오직 하나, 관찰 영역(무대)이다.
무대는 주변 면과 명도가 확실히 갈려야 한다. 나머지는 전부 조용한 회색.

**② 데이터 중심** — 장식용 그라데이션·그림자·아이콘을 넣지 않는다.
모든 픽셀은 "학생이 읽어야 할 값"이거나 "그 값을 읽게 돕는 것"이어야 한다.

**③ 물리적 정확성** — 보이는 것이 곧 계산 결과여야 한다. 예쁘게 보이려고 값을 보정하지 않는다.
근사·가정·무시한 항은 화면이나 활동지에 명시한다.

**④ 암묵적 안내** — "이렇게 하세요" 설명문 대신 인터페이스 자체로 유도한다.
- 슬라이더 범위를 교육적으로 의미 있는 구간으로만 제한한다.
- **첫 화면(기본값)이 이미 "볼 만한 상태"여야 한다.** 빈 화면으로 시작하지 않는다.
- 학생이 무엇을 하든 망가지지 않게 한다. 되돌리기 버튼보다 안전한 범위가 낫다.
- **막다른 선택은 화면이 먼저 알려 준다.** (`press/` 4단계: "이 A₂ 로는 닿을 수 없습니다 → 좁은 쪽으로")

**⑤ 신호 주기** — 지금 중요한 것 하나만 강조한다. 상시로 켜져 있는 강조는 강조가 아니라 배경이다.

**⑥ 끊어 주기** — 긴 과정은 학생이 스스로 넘기는 단계로 자른다. 일시정지·단계 이동을 준다.

## §3. 페이지 레이아웃 표준

페이지는 "화면을 꽉 채우는 앱"이 아니라 "위에서 아래로 읽는 문서"다.
**`100vh`·`position:fixed`·`overflow:hidden` 을 쓰지 않는다.**
(태블릿 주소창이 나타났다 사라지면서 100vh 가 흔들려 아래가 잘린다)

세로 순서 — 이 순서를 지킨다:

```
  [nav.js 가 자동 삽입하는 상단 메뉴]   ← 직접 만들지 않는다
  ├ 1. 제목 (h1) + 한 줄 설명
  ├ 2. 좌석번호 입력 줄
  ├ 3. (탭이 있으면) 탭 + 탭 설명
  ├ 4. ★ 무대 + 조절 변인 ★   ← 화면의 중심. 좌우 2단
  │      좌: 관찰 영역(캔버스)      우: 조절 변인 → 측정값 → 기록
  ├ 5. 그래프 (전체 폭)        ← v1.0 변경: 격자 바깥, 맨 아래
  └ 6. 활동지 (질문 5~9개)
```

**2단 → 1단 전환은 1024px 미만.**
```css
@media (max-width:1023px){ .cols{ grid-template-columns:1fr } }
```

> ★ **v1.0 변경 — 그래프는 2단 격자 바깥에 둔다.**
> 그래프를 왼쪽 열에 넣으면 1단이 됐을 때 **그래프가 무대와 조절 변인 사이에 끼어**
> 순서가 「무대 → 그래프 → 조절 변인」이 된다. §3 의 순서를 어긴다.
> 전체 폭으로 빼면 순서도 지키고 그래프도 커진다. (세션 4에서 실측으로 발견)

**공간 근접 원칙**: 어떤 값의 설명은 그 값 옆에 둔다.
- 나쁨 — 캔버스 아래 "① 파란 입자는 …" 같은 번호 매긴 범례 목록
- 좋음 — 캔버스 안 또는 바로 옆에 색 점과 이름을 붙인 범례(`.legend`)

**분기 (v1.1)** — 조절 변인 중 **무대를 직접 움직이는 것**(예: 가열 출력 → 온도계 액주)은 우측 열 대신 **무대 카드 안**에 둘 수 있다. 인과 근접이 좌/우 분담보다 우선한다. (`liquid/` 전례 · 설계지시안 v3.1 §4-3 ③)

## §4. 컬러 — 토큰만 쓴다

★ **페이지에서 `:root` 를 다시 선언하지 않는다.** 색이 마음에 안 들면 페이지가 아니라
`shared/style.css` 를 고친다. 그래야 사이트 전체가 함께 바뀐다.

```
/* 면 */
--bg     #f5f7fa   페이지 배경 (순백 금지 — 장시간 응시 시 눈부심)
--panel  #ffffff   카드·패널
--line   #e3e6ea   구분선

/* 글자 — 괄호는 흰 카드 위 실측 대비비 */
--t1 #1f2328  제목·핵심 수치        (15.80:1)
--t2 #454b52  본문·레이블            (8.82:1)
--t3 #5b636b  11px 이하 소형 레이블   (6.10:1)
  ⚠ 소형 레이블에 #94a3b8 류의 연한 회색을 쓰지 않는다. 빔프로젝터로 쏘면 사라진다.

/* 데이터 컬러 — 밝은 면(카드·그래프·표) 위 */
--d-blue   #1d4ed8 (6.70:1)  --d-cyan   #0f5c8c (7.17:1)
--d-green  #15803d (5.02:1)  --d-amber  #b45309 (5.02:1)
--d-red    #b91c1c (6.47:1)  --d-violet #6d28d9 (7.10:1)
--d-gray   #5f6b7a (5.43:1)
  의미 배정: 파랑=기본 / 청록=보조 / 초록=안정·정상 / 황갈=주의·중간 /
            적=경고 / 보라=특수 / 회=중성·비활성
  예외 (v1.1): **계측기 지시부**(온도계 액주 등)는 물질 표본이 아니므로 "물질의 실제 색"
  조항의 대상이 아니며, 교실 기구 관습색을 쓸 수 있다. 단 그 페이지 안에서 의미가
  일관돼야 하고(예: `liquid/`의 "빨강 = 열") ΔE·대비 실측을 통과해야 한다.
  ⚠ 이때 `--d-amber`와 `--d-red`는 한 파일에 공존 불가 — 청색약 ΔE 2.9 (`liquid/` 실측)

/* 어두운 무대 위 발광 계열 — 괄호는 #0f172a 대비 실측비 */
--p-sky #56b4e9 (7.74) --p-orange #fb923c (7.89) --p-yellow #facc15 (11.66)
--p-mint #34d399 (9.29) --p-violet #a78bfa (6.56) --p-silver #cbd5e1 (12.02)
  ⚠ 밝은 면용 색을 어두운 무대에 그대로 쓰지 않는다. 반대도 마찬가지다.

/* CPK 원자색 — 국제 표준. 값 변경 금지 */
H #FFFFFF / C #404040 / N #3050F8 / O #FF0D0D / F #90E050
Cl #1FF01F / Na #AB5CF2 / S #FFFF30 / P #FF8000 / Fe #E06633 / Cu #C88033
  밝은 무대에서 H·S·Cl 는 외곽선이 없으면 사라진다.
  규칙: stroke = fill 을 40~60% 어둡게, 알파 0.85, lineWidth 1. 예외 없음.
```

## §5. 무대(관찰 영역)는 두 종류다

**한 페이지 안에서 섞지 않는다.**

**■ 어두운 무대 `.stage.stage--dark`** — 배경 `--stage-dark #0f172a`
- 쓸 때: 입자 운동, 빛·스펙트럼, 궤적, 전자·오비탈, 우주·천체
- `--p-*` 발광 계열을 쓴다. 격자선 `rgba(148,163,184,0.14)`

**■ 밝은 무대 `.stage.stage--light`** — 배경 `--stage-light #ffffff`
- 쓸 때: 유리기구·용액·적정·침전·pH·용해도·기계 장치·광물 표본
- 화면에서 유일한 순백이어야 한다. 격자선 `rgba(40,45,52,0.055)`
- 유리 하이라이트는 흰색이 아니라 옅은 청색 `rgba(160,200,228,0.75)`

**캔버스 필수 처리 (두 모드 공통)**
```js
const dpr = Math.min(window.devicePixelRatio || 1, 2);   // 2 초과는 성능만 먹는다
canvas.width  = wrap.clientWidth  * dpr;
canvas.height = hCss * dpr;
canvas.style.height = hCss + "px";
ctx.setTransform(dpr,0,0,dpr,0,0);
```
→ 이 처리를 빼면 태블릿에서 글자와 원이 뭉개진다.
→ `ResizeObserver` 로 폭 변화 때 다시 잡는다. 창 회전 시 필수.

> ⚠ **숨어 있는 캔버스(`display:none`)는 `clientWidth` 가 0 이다.**
> 반지름·크기를 그 값으로 계산하면 **음수가 되어 `arc()` 가 예외를 던진다.**
> 그리기 함수 첫머리에서 크기가 너무 작으면 그냥 반환하라. (세션 4 실제 오류)

## §6. 타이포그래피

- 폰트는 `shared/style.css` 의 body 스택을 상속. 페이지에서 다시 지정하지 않는다.
- 크기 위계: 제목 20~22 / 소제목 14 / 본문 13.5 / 레이블 12 / 소형 11
- 측정 수치는 22px, `font-weight:600`, **`font-variant-numeric:tabular-nums`**
  ⚠ 없으면 숫자 폭이 달라져 값이 덜덜 떨린다.
- 밝은 배경 위에서 `font-weight:300` 을 쓰지 않는다. 수치·화학식은 400~600.
- 화학식 아래첨자는 `<sub>` 로. 유니코드 ₂ 는 폰트에 따라 크기가 널뛴다.
- 단위는 수치와 다른 크기·색으로 분리한다: `<span class="value">564.3</span><span class="unit">a.u.</span>`

## §7. 조작 요소 규범

**■ 슬라이더 — 물리량마다 색을 배정한다.**
```
온도      class="rng-temp"    한색→난색 4단 그라데이션
부피·공간  class="rng-vol"     연파랑→진파랑
개수·농도  class="rng-count"   연보라→진보라
시간·속도  class="rng-time"    연초록→진초록
```
레이블과 현재값은 슬라이더 바로 위 같은 줄에 좌·우로. 현재값에는 반드시 단위를 붙인다.

⚠ (v1.1) **「압력」 계열 클래스가 없다.** `press/`·`liquid/`는 압력 슬라이더를 **클래스 없음(기본 회색 트랙)**으로 두어 네 번째 구분으로 쓴다. 억지로 `rng-vol`을 주면 부피와 같은 색이 되고, `rng-count`를 주면 틀린 의미가 붙는다. 클래스 신설은 이월 과제.

**■ 숫자 입력칸 (v1.0 신설)** — 학생이 **계산한 값을 직접 넣어야 하는** 시뮬레이션에서는
슬라이더 옆에 `<input type="number">` 를 함께 둔다. 슬라이더로는 원하는 정밀도가 안 나온다.
`press/` 가 이 방식이다(슬라이더 눈금 0.01 L, 숫자칸 0.001 L).
→ 숫자칸도 **16px 이상**이어야 한다(iOS 자동 확대).

**■ 버튼** — 화면에 primary(파란 채움) 버튼은 최대 1개.

**■ 체크박스** — 라벨 전체가 클릭 영역이어야 한다. `<label>` 로 감싸고
**감싼 `<label>` 에 `min-height:44px`** 를 준다. (체크박스 자체만 키우면 미달이다 — 4부 ⑤)

**■ 터치 크기** — `shared/style.css` 의 `@media (pointer:coarse)` 가 처리한다.
★ 페이지 `<style>` 은 공통 CSS 보다 뒤에 오므로 **같은 특이도면 페이지가 이긴다.**
터치 규칙을 덮어쓰지 않도록 페이지에서 `button{}`·`input{}` 같은 **요소 선택자를 쓰지 않는다.**

**■ 키보드** — 슬라이더·버튼은 Tab 으로 닿아야 한다. `outline:none` 으로 포커스 링을 지우지 않는다.

**■ QR 코드 버튼 (v1.1 세션 10 신설)** — 제목(`h1` 등) 오른쪽에 자동으로 붙는다. 페이지에서
직접 만들지 않는다 — `shared/qr.js` 를 4번째 줄로 로드하면 끝이다(§1).
- 내부에 **자체 구현 QR 인코더**(ISO/IEC 18004 · 바이트 모드 · 오류정정 M · 버전 1~10)가
  들어 있다. §1의 「외부 CDN 금지」를 QR 라이브러리에도 그대로 지킨 것 — 학교 망이 CDN을
  막아도, `file://` 로 열어도 QR이 뜬다.
- 로컬(`file://`·`localhost`)에서 열면 실제 URL 대신 `qr.js` 상단의 `PUBLISHED_BASE` 상수 +
  `data-current` 값을 조합해 **배포 주소**를 QR에 넣는다. **배포 주소가 바뀌면 이 상수 한 줄만
  고친다.** (다른 페이지는 손댈 필요 없음 — `shared/style.css` 의 `--accent`와 같은 구조)
- 제목 요소를 못 찾으면(비표준 구조) 버튼을 아예 붙이지 않고 조용히 넘어간다 — 화면을
  깨뜨리지 않는다. 새 페이지가 `.head h1`·`.pg-title`·`header h1`·`h1` 중 어디에도 안 걸리면
  `data-title-sel` 속성으로 선택자를 직접 지정한다.
- 검증: 생성된 QR 매트릭스를 파이썬 `qrcode` 라이브러리와 **비트 단위로 비교**하고, 화면에
  그려진 이미지를 다시 디코딩해 원래 주소가 나오는지 확인한다(§1-3의 「에셋별 단독 검증」과
  같은 원칙 — 눈에 보기 좋은 것과 실제로 읽히는 것은 다르다).

## §8. 측정값 표시

```html
<div class="readout">
  <div class="label">압력 P</div>
  <div><span class="value">564.3</span><span class="unit">a.u.</span></div>
</div>
```
상태색은 클래스로: `.is-ok`(초록) / `.is-warn`(주황) / `.is-bad`(빨강)
⚠ **상태를 색으로만 알리지 않는다.** 짧은 글자를 함께 넣는다("정상" / "과열").

유효숫자를 고정한다. 소수점 자리가 3→4→3 으로 흔들리면 값이 튀어 보인다.

> ★ **측정에 시간이 걸리는 값은 "재는 중"을 표시한다.** (v1.0 신설)
> 입자 충돌을 세어 얻는 압력 같은 값은 조작 직후 몇 초 동안 부정확하다.
> 그 동안 숫자를 그대로 보여 주면 학생이 잘못된 값을 읽는다.
> `gaslaws/` 는 "압력을 재는 중 — 잠시 기다리세요"를 띄우고 **기록 버튼을 거절**한다.
> 이것은 회피가 아니라 **실험 절차 그대로**다(눈금이 안정될 때까지 기다린다).

## §9. 색각·접근성 — 색 개수에 따라 규칙이 다르다

Viénot–Brettel–Mollon 색각이상 시뮬레이션 후 CIELAB ΔE 로 실측한 결과.
(ΔE 20↑ 확실히 구분 / 11~20 주의 / 11↓ 혼동)

```
계열 2개  : 색만으로 구분해도 안전하다.
계열 3개  : 아래 조합을 쓴다.
            어두운 무대 — 하늘#56b4e9 / 노랑#facc15 / 빨강#ef4444   (최악 ΔE 45.9)
            밝은 배경   — 파랑#1d4ed8 / 황갈#b45309 / 검정#1f2328   (최악 ΔE 56.3)
            ⚠ v1.0의 38.2는 오기 — `검토.js` 구현으로 재계산해 56.3으로 정정 (세션 5)
계열 4개↑ : ★ 색만으로는 불가능하다. 반드시 두 번째 채널을 함께 준다. ★
            (모양·패턴·직접 레이블·굵기 중 하나)
```

**실측 함정 — "예뻐 보이는 4색 팔레트"는 거의 전부 여기 걸린다**

| 쌍 | 색각 유형 | ΔE | 판정 |
|---|---|---|---|
| 파랑 `#1d4ed8` ↔ 보라 `#6d28d9` | 녹색약 | **0.4** | 사실상 같은 색 |
| 주황 `#fb923c` ↔ 노랑 `#facc15` | 청색약 | **9.9** | 혼동 |
| 하늘 `#56b4e9` ↔ 연두 `#34d399` | 청색약 | **5.6** | 혼동 |
| 하늘 `#56b4e9` ↔ 은색 `#cbd5e1` | 적색약 | 28.9 | 안전 |

→ **어두운 무대에서 4색이 필요하면 3색으로 줄여라.** `gaslaws/` 는 이 실측 때문에
   무대 색을 **하늘(입자) / 주황(바깥 압력) / 은색(피스톤·눈금·글자) 3색**으로 줄였다.

**두 번째 채널 예시**
- 입자: 원 / 사각 / 삼각 (`.legend .swatch`, `.swatch.sq`, `.swatch.tri`)
- 선: 실선 / 파선 `setLineDash([6,4])` / 점선 `[2,3]` / 굵기
- 원자: 원 안에 원소 기호를 직접 쓴다(가장 확실하다)
- 곡선: 곡선 옆에 이름을 직접 붙인다 (`liquid/` 의 증기 압력 곡선 4개)

**그 밖에**
- 캔버스는 `<canvas>` 만으로 읽히지 않는다. 옆에 텍스트 측정값을 반드시 둔다.
- `<canvas aria-label="...">` 로 무엇을 그리는 중인지 한 줄 설명한다.
- 깜빡임: 1초에 3회 이상 번쩍이는 효과 금지(광과민성 발작 위험).
- ⚠ (v1.1) **`검토.js`의 ΔE·대비 출력은 참고 표시일 뿐 실패로 집계되지 않는다**(4부 ⑪).
  스크립트가 고쳐지기 전까지 이 두 값은 **사람이 직접 판정**하라. 그리고 ΔE 집합은
  손으로 세지 말고 도구가 실제로 스캔하는 방식(소스 정규식 전수)으로 뽑아라(4부 ⑩).

## §10. 애니메이션·성능 — 교실 기기 기준

> ⚠ 체험형 3D의 렌더링 기본값은 §1-4, 에셋 조달은 §1-3. 이 §10의 성능 예산은 두 유형 공통이다.

- `requestAnimationFrame` 루프 하나만. `setInterval` 금지.
- 입자 충돌은 O(N²). N=300 이면 프레임당 약 45,000쌍 — 구형 태블릿에서 끊긴다.
  ★ 슬라이더 max 를 "가장 느린 학생 기기에서 60fps 가 나오는 값"으로 잡는다.
  **성능 문제가 실제 기기에서 확인되기 전에는 공간 분할 최적화를 도입하지 않는다.**
- 탭이 안 보일 때는 루프를 멈춘다:
  ```js
  document.addEventListener("visibilitychange", () => {
    if (document.hidden) { cancelAnimationFrame(rafId); rafId = null; }
    else if (!rafId) rafId = requestAnimationFrame(loop);
  });
  ```
  안 그러면 학생 태블릿 배터리가 녹는다.
- 이징은 ease-out 계열, 120~240ms. 400ms 넘는 UI 전환은 느리게 느껴진다.
- `prefers-reduced-motion` 은 공통 CSS 가 처리한다. **단 캔버스 애니메이션은 CSS 로 못 막으므로**
  JS 에서도 확인해 자동 재생 대신 **일시정지 상태로 시작**한다.

## §11. 활동지·학생 데이터

- 질문은 **5~9개**. 관찰 → 관계 → 설명 → 심화(모형의 한계) 순.
- **마지막 문항은 "이 모형이 실제와 다른 점"을 묻는다.** 근사와 한계를 드러내는 것이 핵심이다.
- 각 문항 앞에 성격 태그를 붙인다: `예측 / 관찰 / 관계 / 설명 / 계산 / 설계 / 통합 / 모형 / 한계`
- **활동지 끝에 「이 모형의 가정과 한계」 목록을 반드시 붙인다.** 번호를 매겨 6~8항목.
- (v1.1) **시연 전용 분기** — 교사 시연용으로 문항을 들어낼 때도 「가정과 한계」는 화면에 남긴다(접힘 허용). 들어낸 문항은 삭제가 아니라 별도 보존을 권한다. (`liquid/` 전례)
- 학생 제출: CSV 내려받기. **이름 대신 좌석번호·익명코드**를 권한다.
  실명 저장은 학교 개인정보 처리 절차 대상이 될 수 있다.
- 서버 저장으로 올라갈 경우(Supabase 등) **RLS 정책을 반드시 건다.**
  안 걸면 학생이 개발자도구로 남의 답을 전부 읽거나 지울 수 있다.

## §12. CSS 작성 규칙 — 실제로 터진 함정

**① 페이지 `<style>` 은 `shared/style.css` 보다 뒤에 온다. 특이도가 같으면 페이지가 이긴다.**
- 공통 CSS 의 터치 규칙에는 `html` 접두어가 붙어 있다(`html input[type=text]{...}`).
  **이 접두어를 지우면 iOS 자동 확대 버그가 되살아난다. 건드리지 말 것.**
- 페이지에서는 요소 선택자(`button{}`, `input{}`) 대신 **클래스**를 쓴다.

**② 색을 페이지에 직접 적지 않는다.** `var(--d-blue)` 처럼 토큰으로.
토큰에 없는 색이 필요하면 그건 대개 "필요 없는 색"이다.

**③ 캔버스 안 색은 CSS 변수를 JS 에서 읽어 쓴다.**
```js
const CSSV = n => getComputedStyle(document.documentElement).getPropertyValue(n).trim();
const C = { blue: CSSV("--d-blue"), sky: CSSV("--p-sky") };
```

**④ ★ 의사 요소(`::-webkit-slider-thumb` 등)의 특이도를 잘못 세지 마라.** (v1.0 신설)
```css
input[type=range]::-webkit-slider-thumb{...}   /* 특이도 (0,1,1) */
.rng-temp::-webkit-slider-thumb{...}           /* 특이도 (0,1,0)  ← 진다! */
```
`[type=range]` 는 **속성 선택자라 클래스와 같은 급**이다. 그래서 일반 규칙이 클래스 규칙을 이긴다.
**반드시 `input[type=range].rng-temp::-webkit-slider-thumb` 처럼 접두어를 붙인다.**
→ 이것 때문에 물리량별 슬라이더 색이 **사이트 전체에서 한 번도 적용된 적이 없었다.** (4부 ④)

## §13. 단계식 공개·정답 게이팅 (세션 11 신설)

한 화면에 정보가 너무 많아 3초 규칙(P4-B1)이 깨지면 **단계 탭**으로 쪼갠다 — 단계마다 질문 하나,
그 질문에 필요한 요소만 보인다. `liquid/` 4단계 개편(관찰 → 무엇이 바꾸나 → 양은 상관없나 → 자유 탐구)이 본보기다.
규칙 다섯:

**① 표시 여부는 단일 원천 표 하나로 정한다.** 단계×요소 가시성 표(`STAGE`/`SHOW`)를 `sim.js` 한 곳에
두고, `applyStage()` 만 `display` 를 쓴다. 흩어진 `display` 대입은 게이팅을 조용히 우회한다 —
실측: 차트 모드 토글과 `drawThermo()` 안의 무조건 `display` 대입 2곳이 정답 게이트를 뚫고 있었다(F-1 단일 원천).

**② 잠금은 DOM 에 먼저 쓴다.** 루프가 매 프레임 슬라이더의 `.value` 를 직접 읽는 구조면,
상태 객체에 잠금값을 저장해도 **아무 효과가 없다.** 잠글 값을 input 의 `.value` 에 먼저 쓰고,
나머지 코드는 평소처럼 DOM 을 읽게 둔다.

**③ 정답 숨김은 그리기 자체를 건너뛴다.** 캔버스는 DOM 으로 검사되지 않고, `display:none`·인라인
스타일은 개발자도구로 뒤집힌다. `gated()` 분기로 **그리는 코드를 스킵**한다. 누출 경로는 감이 아니라
**소스 전수 탐색으로 목록화**한다 — 실측: 설계 단계 11곳 → 소스 직독으로 3곳 추가 발견
(가열 곡선의 끓는점 라벨 / 곡선 **이름표의 위치** 자체가 760mmHg 교점을 가리킴 / 현재 압력 교점 점).
숨긴 값은 라벨·aria-label·CSV 헤더·결과표 열까지 같이 숨긴다(J-N 적용 범위와 동일).

**④ 잠긴 변인은 지우지 말고 숨긴다.** 순차 확장 예정 기능은 마크업을 남기고 표시만 끈다(사용자 지시).
기존 검증 스크립트가 마크업 존재를 검사하면 클래스 제거 대신 인라인 `display` 로 덮는다.
단 `style.display=""` 는 클래스 규칙을 못 이긴다 — 명시적 값(`"block"` 등)을 쓴다.

**⑤ 슬라이더 하한·상한은 물리가 정한다.** 범위 끝에서 모형이 표현 못 하는 상태가 되면
(예: 하한 압력에서 실온 이하 끓음 — 가열 전용 모형 밖) **종별 하한**(`pextMin`)을 두고
그 물리적 근거를 코드 주석에 적는다. M8(범위=세계)과 짝 — 왜 여기까지인지가 코드에 남아야
다음 세션이 임의로 범위를 넓히지 않는다.

## §14. 입자 모형 — 거시↔분자 전환 · 렌더러 이식 · 동적 평형 표현 (2026-08-24 신설)

`liquid/` 5탭 개편(닫힌 용기 동적 평형 신설·분자 확대 3D 교체)에서 확립된 규칙.
사용자가 결과를 승인하고 일반 규칙으로 승격을 지시했다.

**① 거시 관찰이 기본, 분자 화면은 학생이 스스로 전환한다.**
입자 수준을 다루는 화면도 첫 화면은 **거시 세계**(비커·용기·수위)다 — 학생이 실험실에서 실제로
보게 될 것을 먼저 보이고, 「분자 크기로 확대해 보기」 버튼으로 분자 화면과 오간다.
분자 화면을 기본으로 깔면 관찰(무엇이 일어나는가)과 설명(왜 일어나는가)의 순서가 뒤집힌다.
- 두 화면은 **같은 모형 상태 하나를 읽는다.** 전환은 렌더러 교체일 뿐 실험은 계속 돈다 —
  전환하는 순간에도 이어진 상태가 보여야 「같은 현상의 두 배율」로 읽힌다.
- 전례: `liquid/` 1단계(비커 ↔ 분자)·2단계(두 용기 ↔ 분자). 토글은 버튼·플래그 하나(`zoom`)를 공유한다.

**② 렌더러는 새로 짜지 말고 이식·재사용한다.**
- **공-막대 3D가 필요하면 `mineral/sim.js`의 「프로그램 ②」(구 임포스터 + 막대 띠 + 화가 알고리즘)를
  이식한다.** `liquid/`의 `M3D`(≈200줄 축약판: 공·막대·판 3종 + 알파 블렌딩)가 두 번째 인스턴스이므로
  다음부터는 그쪽을 복사하는 것이 더 빠르다. 새 임포스터 셰이더를 처음부터 쓰지 마라.
- **기존 셰이더로 그릴 수 있는 변형이면 uniform을 추가해 재사용한다.** 같은 프로그램을
  `viewport`+`scissor`로 한 캔버스에 여러 번 돌리면 무대 여러 개가 나온다 — WebGL 컨텍스트를
  늘리지 않는다(§1-4 상한 16개). 전례: 1단계 비커 셰이더에 `lid`(뚜껑)·`mist`(증발 김) 2개를 더해
  2단계의 열린/닫힌 용기 두 개를 그렸다.
- ⚠ 재사용할 때 **기존 호출부에 새 uniform 기본값을 명시로 넣는다**(`lid=0`·`mist=0`) —
  안 넣으면 이전 프레임 값이 남아 다른 단계의 화면이 오염된다.

**③ 동적 평형 표현 3칙.**
1. **수렴은 지수 해로 쓴다** — `P = P목표 + (P − P목표)·e^(−k·dt)`. 오일러 적분(`P += k·(P목표−P)·dt`)은
   큰 dt에서 진동·발산한다. 지수 해는 어떤 dt에서도 단조 수렴이라 검산(진동 없음)이 구조적으로 통과한다.
2. **평형에서도 교환을 계속 그린다** (M3 「정지=평형」 방지) — 개수가 목표에 닿은 뒤에도
   증발·응축 쌍 이벤트를 주기적으로 일으킨다. 화면이 멈추면 평형이 「끝난 상태」로 읽힌다.
3. **평형 결론 문구는 «지금 실제로 평형인 순간»에만 띄운다** (J-N5) — 조건(온도)을 바꾸면 결론이
   사라졌다가 새 평형에서 다시 뜬다. 「도달하면 끝」이 아니라 조건에 반응하는 상태임이 문구의
   등장·소멸 자체로 보인다. 판정 함수(`sealedAtEq`) 하나를 카운터·상태·결론이 같이 읽는다(F-1).

**④ 단위 병기는 단일 원천 함수로.** 「1.00 atm (760 mmHg)」처럼 두 단위를 병기하기로 했으면
표기 함수 하나(`setPress`)를 만들어 모든 readout·상태 문구가 그것만 부른다. 흩어진 `toFixed` 조합은
자릿수·괄호 형식이 화면마다 어긋난다.

---

# 3부 · 검증 스크립트

## 파일 위치

전부 `[AI] 시뮬레이션 사이트/검증스크립트/` (저장소 **바깥**).

| 파일 | 용도 |
|---|---|
| `검토.js` | ★ **P3 + P4-A + P6-C4 를 한 번에.** `render.js` 를 대체한다 |
| `<이름>_check.js` | P1 수치 검증 (gaslaws / press / liquid / mineral) |
| `topology_check.js` (v1.1 세션 6 신설) | **구조 위상 + 겹침 검사.** 격자·결정 구조 시뮬레이션에서 결합선이 실제로 옳은 원자쌍만 잇는지·고립점 비율·연결 덩어리 비율을 확인하고, 원자쌍 겹침(overlap) 0건을 함께 확인한다 |
| `<이름>_core.js`, `gaslaws_engine.js` | 각 `sim.js` 의 **계산부와 같은 내용**. 고쳤으면 여기도 반영 |
| `check.js` | 세션 3의 수치 검증 41항목 (vapor·osmosis·entropy·rate) |
| `render.js` | 세션 3의 렌더링 실측. `검토.js` 로 대체됐으나 남겨 둠 |
| `사용법_신규4종.md` | 실행법 요약 |

## 실행

```bash
# ── P1 수치 (화면 만들기 전 · 코드 고친 뒤) ──
cd "<...>/검증스크립트"
node gaslaws_check.js     # 29항목
node press_check.js       # 31항목
node liquid_check.js      # 105항목 (세션 11: 38→75 · 2026-08-24 5탭 개편: 75→105)
node mineral_check.js     # 26항목

# ── P3+P4-A+P6 렌더링 (테스트 서버를 먼저 띄운다) ──
S="<세션경로>/mnt/[AI] 시뮬레이션 사이트/화학탐구실"
mkdir -p /tmp/testsite && cp -r "$S"/* /tmp/testsite/
cd /tmp/testsite && python3 -m http.server 8765 &

cd "<...>/검증스크립트"
NODE_PATH=/home/claude/.npm-global/lib/node_modules \
PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers \
node 검토.js gaslaws press liquid mineral

# ── P4-B 육안 ──
# /tmp/shots/*.png 를 반드시 Read 로 열어 본다
```

> **헤드리스 브라우저는 이 환경에 이미 있다.** `/opt/pw-browsers` + `~/.npm-global` 의 playwright.
> **`playwright install` 을 하지 말 것.**

## 스크립트를 쓸 때 알아 둘 것

- **입자 운동을 실제로 돌리는 검증(gaslaws)은 실행할 때마다 값이 조금씩 다르다.**
  통과 기준에 그만큼 여유를 두었다. 한 번 실패하면 두세 번 더 돌려 보라. 계속 실패하면 진짜 회귀다.
- **검증도 화면과 같은 방식으로 읽어야 한다.** 화면이 "4초 기다렸다 긴 평균으로 읽는" 값을
  검증에서 한 순간만 재면 간헐적으로 실패한다. (세션 4에서 실제로 겪음)
- `검토.js` 는 **숨어 있는 캔버스를 건너뛴다.** 크기 0인 캔버스를 재면 가장자리 검사가 오탐한다.
- (v1.1) **실행 전 포트 충돌을 확인하라.** 이전 세션의 서버가 8765에 살아 있으면 404가
  "JS오류 · 토큰 0개"라는 그럴듯한 검사 실패로 위장된다(4부 ⑮).
- (v1.1) **ΔE·대비는 출력만 되고 `fails`로 집계되지 않는다**(4부 ⑪). 사람이 직접 판정할 것.
- (v1.1) **A1(변화 감지)은 상시 애니메이션 페이지에서 무의미하다** — 슬라이더와 무관하게 항상 2%를 넘는다. 판정 근거로 쓰지 마라.
- (v1.1 세션 11) **`검토.js` 는 탭을 JS 오류 수집용으로만 클릭한다.** 전 탭을 클릭한 뒤 tab[0]으로 돌아와 "조작 후"를 측정하므로, **탭(단계)별 오버플로·터치 타깃 실측이 아니다.** 단계 탭 페이지는 별도 스크립트로 단계마다 재측정해야 한다(`검토.js` 는 5개 시뮬 공용이라 수정 금지).
- (v1.1 세션 11) **`page.screenshot({fullPage:true})` 를 한 번 부르면 그 컨텍스트의 `(pointer:coarse)` 에뮬레이션이 영구히 풀린다**(이 환경의 Chromium 실측). 측정 컨텍스트와 스크린샷 컨텍스트를 분리하고, 측정 로그에 coarse 플래그를 함께 찍어 오염을 자가 검출하라.
- (2026-08-27 redox) **`검토.js`의 「JS오류 셰이더 1281/1282」는 대개 «프로세스의 첫 GL 컨텍스트»
  아티팩트다.** `--use-gl=swiftshader` 로 띄운 첫 페이지에서만 나고 화면은 무결하다. 판별법:
  **인자 순서를 바꿔** 다른 시뮬을 먼저 돌려 본다(`node 검토.js torricelli redox`) — 경고가 첫
  자리로 따라가면 대상 시뮬의 결함이 아니다. redox 실측: 단독 실행 시 360폭에 8건, 순서를 바꾸면
  redox 0건·torricelli 0건. 공용 스크립트라 수정 금지 → 하네스 쪽 대응은 이월한다.
- (2026-08-27 redox) **`검토.js` 는 WebGL 캔버스를 측정하지 않는다**(`getContext("2d")`가 null 이라
  `painted=-2`). 「빈 캔버스 0」은 «0건»이 아니라 «미측정»이다. 3D 시뮬은 가장자리 잘림·빈 화면을
  전용 프로브(씬 객체를 카메라로 투영해 화면 밖 앵커를 세는 방식)로 따로 재야 한다.
- (v1.1 세션 11) **`<이름>_core.js` 는 `sim.js` 계산부의 수동 복제다.** 계산부를 고쳤으면 검증을 돌리기 전에 `diff` 로 두 파일의 계산부가 동일함을 확인하라 — 동기화를 빼먹으면 낡은 코어가 통과해 회귀가 숨는다.
- (v1.1 세션 11) **검사를 추가할 때는 최종 요약 출력 직전에 넣어라.** `process.exit()` 뒤에 넣으면 영원히 실행되지 않는다(한 세션에서 2회 재발 — 4부 ㉑). 추가 후 실행해 통과+실패 합계가 추가한 개수만큼 늘었는지 확인한다.

---

# 4부 · 이 사이트에서 실제로 터진 함정

**추측이 아니라 전부 실제로 일어난 일이다.** 새 시뮬레이션을 만들 때 같은 데 빠지지 마라.

## ① 로드맵이 정한 도구의 목적을 어겼다 (세션 4 · 가장 큰 것)

`gaslaws/` 를 처음에 **실제 기체 모형**(입자에 크기가 있는 MD)으로 만들었다.
물리는 정확했지만 샤를 그래프의 외삽선이 **−296 ℃**로 나왔다. 3차시의 목표는 **−273 ℃**를
학생이 스스로 발견하는 것인데, 그것이 불가능해진 것이다.

원인은 물리가 아니라 **설계 판단**이었다. 로드맵 §4 가 이미 "이 도구는 이상 기체 모형으로
목적을 한정하고, 비이상성은 실측 데이터 표로 다루라"고 적어 두었는데 그것을 어겼다.

**엔진을 이상 기체 모형으로 다시 쓰자 외삽선이 정확히 −273.0 ℃가 되었다.**

> **교훈**: P0 에서 "로드맵이 이 도구를 어디에 쓰라고 했는가"를 먼저 확인하라.
> 물리를 정확히 짜는 것과 교육적으로 맞는 것은 다른 문제다.

## ② 평균만 재는 검증은 진동을 못 잡는다 (세션 4)

피스톤이 평형에서 **부피가 ±17.6 % 로 출렁이고** 있었는데, 평균값을 재는 수치 검증은 전부 통과했다.
스크린샷을 보고서야 발견했다.

원인: **위치 제어가 압력 센서보다 빨랐다.** 센서가 따라오기 전에 피스톤이 움직여 진동했다.

> **교훈**: 되먹임이 있는 시스템은 **"시간에 따른 흔들림"과 "응답 시간"을 반드시 따로 재라.**
> 그리고 구동은 센서보다 **느려야** 한다.

## ③ 그림이 물리를 배신했다 (세션 4)

`press/` 의 유압유를 처음엔 **끊긴 두 기둥**처럼 그렸다. 파스칼 원리("압력은 액체 전체에 같게
전달된다")를 가르치는 화면인데, 그림이 "액체가 이어져 있지 않다"고 말하고 있었다.
ㄷ 자로 이어진 통으로 다시 그렸다.

> **교훈**: P5-M1(은유 오염). **그림이 개념과 모순되지 않는지** 스크린샷으로 확인하라.

## ④ CSS 특이도 — 슬라이더 색이 한 번도 안 먹고 있었다 (세션 4)

`shared/style.css` 의 물리량별 슬라이더 색(`.rng-temp` 등)이 **사이트 전체에서 한 번도
적용된 적이 없었다.** `input[type=range]::-webkit-slider-thumb`(0,1,1)가
`.rng-temp::-webkit-slider-thumb`(0,1,0)를 이겼기 때문이다.

3배 확대 스크린샷을 찍어 "세 슬라이더가 완전히 똑같다"는 것을 보고 발견했다.
접두어를 붙여 고쳤고, 그 결과 `vapor`·`osmosis`·`entropy`·`rate` 4개 페이지의 겉모습도 함께 바뀌었다.

> **교훈**: 의사 요소 규칙은 **특이도를 손으로 세어 보라.** `[type=range]` 는 클래스와 같은 급이다.

## ⑤ 체크박스 터치 영역이 24px 이었다 (세션 3)

공통 CSS 의 `@media (pointer:coarse)` 는 체크박스 **자체**만 24px 로 키운다.
실제 클릭 영역인 `<label class="checkrow">` 에는 아무 규칙이 없어 44px 기준에 미달했다.
→ 각 페이지의 `.checkrow` 에 `min-height:44px` 를 추가해 해결.
→ **검증 스크립트의 판정 기준도 함께 고쳤다.** 체크박스는 감싼 `<label>` 의 크기로 재야 한다.

## ⑥ 곡선이 잘려 "평평한 고원"으로 보였다 (세션 3)

`rate/` 확대 모드에서 세로축만 늘였더니 봉우리가 잘려 분포로 읽히지 않았다.
→ **가로축도 함께 좁혀** 두 꼬리 면적이 눈에 띄게 다르도록 해결.

## ⑦ 캔버스 글자가 오버레이와 겹쳤다 (세션 3)

`entropy/`·`osmosis/` 에서 캔버스에 그린 글자가 `.stage-note` 와 겹쳤다.
→ 요약 문구를 관찰 상자 **안쪽** 아래로 옮겨 해결.
→ **v1.0 권장: 상태·설명 문구는 캔버스에 그리지 말고 HTML 로 캔버스 밖에 둔다.**
   `gaslaws/` 의 압력 비교 막대가 이 방식이다.

## ⑧ 휴대폰 폭에서 글자가 상자를 넘쳤다 (세션 4)

360px 에서 안내판 글자가 상자 밖으로 나갔다.
→ `ctx.measureText()` 로 **실제 글자 폭을 재서** 판 크기를 정하고,
   좁으면 **짧은 문구로 바꾸도록** 했다.

> **교훈**: 캔버스 안 글자는 **반드시 폭을 재라.** CSS 와 달리 자동으로 줄바꿈되지 않는다.

## ⑨ 조명이 물체와 함께 돌았다 (세션 4 · WebGL)

`mineral/` 광물 표본이 회전할 때 **어두운 올리브색**으로 보였다.
법선을 **물체 좌표계**에서 계산해 놓고 조명은 세상 좌표계 방향을 썼기 때문에,
표본이 돌면 빛도 같이 돌았다.
→ 회전 행렬의 역행렬로 법선을 되돌려 해결. **GLSL ES 1.0 에는 `transpose()` 가 없어 직접 만들었다.**

## ⑩ 색각 ΔE 계산에서 네 번째 색을 아무도 안 셌다 (세션 5)

`liquid/` 액주 색을 정할 때 설계자·검토자·오케스트레이터 셋 모두 "이 페이지의 데이터 토큰은 3개"라는 전제로 ΔE를 계산했다. 실제로는 CSV 저장 성공 메시지가 `--d-green`을 참조하고 있었고, `검토.js`는 **소스를 정규식으로 전수 스캔**하므로 그 색도 집합에 들어간다.
결과: **개선 전 페이지는 처음부터 ΔE 14.1로 P6-C4 미달**이었는데 아무도 몰랐다. 실행자가 "실측 22 ≠ 예고 38.3"의 불일치를 보고해서야 드러났다.

> **교훈**: 측정 집합을 손으로 세지 마라. **도구가 실제로 세는 방식 그대로** 뽑아서 계산하라.

## ⑪ "전체 통과 — 문제 0건"이 미달을 덮고 있었다 (세션 5)

`검토.js`는 색각 ΔE와 배경 대비를 **계산해서 출력만 하고, 실패 카운터(`fails`)에 넣지 않는다.** 그래서 ΔE가 0.8이어도 "■ 자동 검사 전체 통과 — 문제 0건"이 찍힌다. README의 검증 결과표가 이 상태로 쌓여 왔고, `liquid/`의 미달(14.1)을 구조적으로 덮고 있었다.

> **교훈**: 자동 검증의 "통과"를 믿기 전에 **그 스크립트가 무엇을 실패로 세는지 코드로 확인하라.** 측정하는 것과 판정하는 것은 다르다.

## ⑫ 시연 각본을 "끓기 시작"까지만 검산했다 (세션 5)

10분 시연 각본의 3단(에터 ×10 배속)은 화면상 0.8초 만에 끓기 시작하니 여유롭다고 검산됐다. 그런데 **9.3초 만에 다 증발**해 액주가 실온으로 되돌아가고, 남은 80초는 빈 비커가 된다. "끓기까지"만 재고 "다 마를 때까지"(`toDry`)를 안 쟀다. 배속을 ×3으로 내리고 정지 지시를 넣어 해결.

> **교훈**: 시간이 흐르는 시나리오는 **종료 상태까지** 검산하라. 시작 조건 통과가 시나리오 성립을 보장하지 않는다.

## ⑬ 손으로 옮겨 적은 수치가 두 번 틀렸다 (세션 5)

지시안에 78.4(문헌값)를 계산값 자리(78.3)에 적었고, 검토자가 그걸 잡아 고쳤다. 그런데 **바로 다음 개정에서** 81.720을 81.6으로 또 잘못 옮겨 적었다. 같은 유형이 잡힌 직후에 재발한 것이다.

> **교훈**: 체크리스트·각본의 기준 수치는 손으로 옮기지 말고 **계산 명령을 함께 적어 재생성**하게 하라. `node -e '...boilingPoint(byId("water"),0.5)'` 한 줄이면 된다.

## ⑭ 한 가지 판정을 CSS와 JS가 따로 내렸다 (세션 5)

온도계 레이블 표기(풀네임/약칭)는 JS의 `measureText`가, 범례 표시는 CSS 미디어쿼리가 각자 판정했다. 두 판정이 어긋나 1194px에서는 약칭이 떴는데 범례가 숨었고, 360px에서는 범례가 약속한 약칭(`탄`)이 화면에 없었다. 판정을 JS 한 곳으로 모아 해결.

> **교훈**: 하나의 표시 판정은 **한 곳에서** 내리고 나머지는 그 결과를 받아 쓰게 하라. 부수 교훈 — `measureText`는 **굵은 글꼴(선택 상태) 기준**으로 재라. 실기기 한글 볼드는 보통 굵기보다 넓다.

## ⑮ 죽은 테스트 서버가 "정상적인 검사 실패"처럼 보였다 (세션 5)

전 종 재점검의 첫 실행에서 9종 중 8종이 "JS오류 · 토큰 0개"로 나왔다. 진짜 원인은 이전 작업의 `http.server`가 같은 8765 포트에 살아남아 **다른 폴더를 서빙**한 것 — 404가 검사 실패로 위장됐다.

> **교훈**: 검증 스크립트를 돌리기 전에 **포트·서버 상태부터 확인**하라. 실패가 그럴듯해 보일수록 환경을 먼저 의심하라.

## ⑯ 폴백 테스트가 폴백 안내문을 실행 못 하게 막았다 (세션 5)

WebGL 폴백을 재현하려고 `initGL()` 첫 줄에 `return false`를 넣는 sed 기법을 썼는데, 그러면 함수 안의 "gl이 null이면 안내문 표시" 코드 **자체가 실행되지 않는다.** 안내문 노출은 이 기법으로는 영원히 검증 불가였고, 실행자가 원본 대조로 기법의 한계임을 입증하고 "미검증"으로 정직하게 보고했다.

> **교훈**: 폴백을 재현하려면 **폴백 경로 바깥**을 막아라 — `getContext("webgl")` 자체가 null을 돌려주게 오버라이드한다. 테스트 장치가 테스트 대상을 우회하면 그 검증은 성립하지 않는다.

## ⑰ 격자 결합선이 값은 맞는데 위상이 틀렸다 (세션 6)

`mineral/` 을 6종에서 9종으로 늘리는 S-검토에서 격자 결합선이 잘못된 원자쌍을 잇는 오류가 나왔다.
원자 반지름·좌표는 종마다 정확했는데, **어느 점과 어느 점을 선으로 이을지 정하는 규칙**이 틀려 있었다.
수치 검증(P1)과 육안 검증(P4-B)이 둘 다 이걸 못 잡았다 — 값과 스크린샷만으로는
"몇 번째 원자와 몇 번째 원자가 이어졌는가"를 판정할 수 없기 때문이다.
`topology_check.js` 를 새로 만들어 종마다 결합선이 실제로 옳은 원자쌍만 잇는지, 고립점(연결 안 된 점)
비율, 연결 덩어리(그물) 비율을 자동 검사하게 했다.

> **교훈**: 구조를 그리는 시뮬레이션은 **좌표·수치 검증과 별개로 위상(연결 관계) 검증을 둔다.**
> 그림이 그럴듯해 보여도 결합선을 잇는 규칙 자체가 틀릴 수 있다.

## ⑱ 구리·철 원자가 3D 에서 서로 겹쳐 있었다 (세션 6)

금속 2종(구리·철)의 결정 구조를 배치할 때 격자점 좌표·격자 상수는 정확했지만, 일부 원자쌍이
반지름 합보다 가깝게 배치돼 화면에서 겹쳐 보이는 지점이 있었다. 밀도 대조(P1)는 격자 상수만으로
계산하므로 이런 겹침을 잡지 못한다. `topology_check.js` 에 겹침 검사를 추가해 모든 원자쌍의
거리를 반지름 합과 비교, 겹침 0건을 확인했다.

> **교훈**: 3D 배치 좌표는 **밀도 계산과 별개로 원자쌍 거리 전수 검사가 필요하다.**
> 밀도·격자 상수가 맞아도 개별 좌표는 겹칠 수 있다.

## ⑲ 버그를 고친 수정이 다른 곳을 두 번 연달아 깨뜨렸다 (세션 6)

S-검토에서 반려된 항목(⑰의 위상 오류 등)을 고쳐 재검토했더니, 이번엔 흑요석의 그물 렌더링이
무너지는 새 회귀가 나왔다. 그것도 고쳐 다시 재검토했더니 석영의 산소(O) 기호가 화면에서 사라지는
또 다른 회귀가 나왔다. 두 번 모두 "방금 고친 부분"만 좁게 재확인했다면 놓쳤을 문제였다 —
스크린샷 세트 전체를 다시 본 것으로 잡았다.

> **교훈**: 수정 후 재검증은 **고친 부분만이 아니라 P4-B 스크린샷 세트 전체를 다시 본다.**
> 국소 수정이 무관해 보이는 다른 종·다른 화면을 깨뜨릴 수 있다.

## ⑳ 「녹는점」이라는 낱말 자체가 오개념이었다 (세션 6)

드라이아이스는 1기압에서 액체를 거치지 않고 승화한다. 그런데 화면에 "녹는점 −78.5 ℃"라고 적어,
일어나지 않는 현상(녹음)에 값을 붙이는 오류가 났다. 다이아몬드도 1기압에서는 녹기 전에 흑연으로
변하므로 "녹는점 3550 ℃"만 단독으로 적으면 틀린다. 녹는점·승화·연화 구간처럼 종마다 다른
상변화 어휘를 숫자 필드 하나로 채우던 것을, **표시 전용 문구 필드**(`mpText`) 하나로 분리해
화면 두 곳(정보창·클릭 정보)이 그 필드 하나만 읽도록 고쳤다.

> **교훈**: 상변화 라벨은 **종류 자체가 다를 수 있다(녹음/승화/연화).** 숫자 하나로 통일해 채우지
> 말고, 문구 전체를 표시 전용 필드로 분리해 단일 원천에서 관리하라(house-rules F-1, §11 단일 원천 원칙과 같은 취지).

## ㉑ process.exit() 뒤에 넣은 검사가 영원히 실행되지 않았다 (세션 11 · 2회 재발)

`liquid_check.js` 에 새 검사를 추가하면서 파일 끝 — 최종 요약과 `process.exit(fail?1:0)` **뒤** — 에
붙였다. 추가한 검사는 한 번도 실행되지 않았고, 낡은 통과 수(38/38)가 그대로 찍혀 "전부 통과"로
보였다. P-검토가 잡아 고쳤는데, **같은 세션의 다음 개정에서 같은 실수가 재발**했다.

> **교훈**: 검사는 최종 요약 출력 **직전**에 넣는다. 추가한 뒤 실제로 실행해
> **통과+실패 합계가 추가한 개수만큼 늘었는지** 대조한다. 합계가 안 늘었으면 검사는 없는 것이다.

## ㉒ 배속 안내 문구가 물에서 떠 "물이 빨리 끓는다"를 재이식했다 (세션 11 · 최중대)

배속을 낮출 때 띄우는 안내("이 액체는 매우 빨리 끓습니다")의 조건이 `next < prev` 뿐이라,
배속을 ×40으로 올려 둔 뒤 **물**을 고르면 — 네 액체 중 가장 느리게 끓는 물에서 — 그 문구가 떴다.
이 시뮬 전체가 싸우는 오개념(★ "물이 빨리 끓는다")을 안내 문구가 도로 심는 구조였다.
같은 부류로, 일시정지 중 압력을 올려도 "포화 증기 압력이 외부 압력과 같아졌습니다"가
수치(251 vs 1140 mmHg)와 모순된 채 남아 있었다(`reBoil()` 미호출).

> **교훈**: J-N5 — 조건부 안내·상태 문구는 **모든 조작 경로**(값을 올렸다 내리기, 정지 중 변경,
> 액체 교체)에서 같은 화면의 수치와 모순되지 않는지 검증하라. 문구를 띄우는 조건과
> 문구가 참인 조건은 다르다.

## ㉓ 나란히 비교 설계가 물리적으로 성립하지 않았다 (세션 11)

"20mL 와 200mL 를 나란히 끓여 둘 다 끓는 상태를 비교한다"는 설계가 지시안에 들어갔는데,
계산해 보니 20mL 가 **다 증발한 뒤에야**(17.3s < 22.3s) 200mL 가 끓기 시작했다. 비교하려던
장면은 어떤 조작으로도 화면에 존재할 수 없었다. P-검토의 계산 검산이 잡았고, "끓기 시작한
**순간의 값을 동결**해 비교"하는 구조로 재설계했다.

> **교훈**: 시나리오는 시작 조건이 아니라 **종단 상태까지** 계산 코어로 검산하라(⑫의 설계 단계판).
> 화면에 담으려는 장면이 물리적으로 공존 가능한지부터 계산이 답한다.

## ㉔ 정답 숨김이 14곳으로 새고 있었다 — 3곳은 소스 직독으로만 발견 (세션 11)

자유 탐구 단계의 "정답 보기" 게이트를 설계하며 누출 경로 11곳을 목록화했는데, P-검토가
`sim.js` 를 직독해 3곳을 더 찾았다: 가열 곡선의 끓는점 라벨, 증기 압력 곡선 **이름표의 위치**
자체(760mmHg 교점 바로 위 — 점을 지워도 이름표가 그 자리를 가리킴), 현재 압력선 교점 점
(압력이 1.00atm이면 정답 점과 일치). 문구·요소만 세면 위치·좌표로 새는 경로를 놓친다.

> **교훈**: 게이팅 누출 경로는 **소스 전수 탐색**으로 목록화하라(§13-③). "무엇이 보이는가"만이
> 아니라 "**어디에** 그려지는가"도 정답을 누설한다.

## ㉕ 결론 문구의 "증거"가 정의상 항상 참이었다 (세션 11)

나란히 비교의 결론을 "끓기 시작 시점의 포화 증기 압력도 같습니다"로 썼는데, 끓기 시작하는
순간의 포화 증기 압력은 **정의상** 외부 압력과 같으므로 이 문장은 어떤 실험에서도 참이다 —
"양과 무관"의 증거가 아니다. 게다가 화면의 「남은 액체 0mL vs 198mL」 수치와 나란히 놓여
모순처럼 읽혔다. 같은 온도(60·80℃)에서 두 비커의 증기 압력을 직접 대조하는 구조로 재작성했다.

> **교훈**: 결론 문구의 증거가 **그 실험이 아니어도 항상 참**이면 증거가 아니라 동어반복이다.
> "이 값이 달랐을 수도 있었는가"를 물어 판별하라.

## ㉖ 역산으로 만든 실세계 수치가 틀렸다 (세션 11)

0.33atm에 해당하는 고도를 표준대기 공식으로 역산해 "에베레스트 8,432m"라 적었다. 실제 정상은
**8,849m**이고, 0.33atm은 환산값이 아니라 정상에서의 **실측값**(West et al. 1999 — 열대 고공
대기가 표준대기와 달라 표준 환산 0.31atm보다 높다)이었다. 그럴듯한 역산이 두 사실을 동시에 가렸다.

> **교훈**: 실세계 수치는 공식 역산으로 만들지 말고 공인값을 검색으로 확인하라. 그리고 화면·한계
> 목록에 **환산값인지 실측값인지** 출처 구분을 병기하라(한계 ⑬ 전례).

## ㉗ FAIL 주입이 약해서 "검사가 잡는다"를 오판할 뻔했다 (2026-08-24)

신설한 동적 평형 검사 14항목의 민감도를 확인하려고 수렴식을 오일러 근사로 바꿔 주입했는데,
계수가 작아 **여전히 수렴**했고 물리 검사 14개는 전부 침묵한 채 계산부 동기 검사(⑩) 하나만 걸렸다.
"주입했더니 FAIL이 났다"만 보고 넘어갔으면 물리 검사들이 아무것도 못 잡는 상태를 「검증된 검사」로
믿었을 것이다. 계수를 90배로 키워 진동·발산을 만들자 8개가 걸렸고, 열린 용기 소진 하한을 0.5 mL로
막는 두 번째 주입으로 나머지 3개가 걸리는 것까지 확인했다.

> **교훈**: FAIL 주입은 **어떤 검사가 걸렸는지까지** 확인해야 완성이다. 엉뚱한 검사 하나만 걸린
> 주입은 민감도 확인이 아니다. 주입은 검사군별로, 걸릴 만큼 세게 넣어라.

## ㉘ 액면 최소 매핑이 「모두 증발」 문구와 모순됐다 (2026-08-24)

비커 셰이더의 액면 높이 매핑(`0.06 + 0.80·fill`)이 fill = 0에서도 6 % 높이의 액체 조각을 남겼다.
1단계의 「다 끓음」에서는 아무도 못 잡고 넘어왔는데, 2단계 열린 용기의 「모두 증발함」 옆에
물이 남아 보이는 순간 화면 수치와 그림이 정면으로 모순됐다. fill ≤ 0.001이면 액면 자체를
그리지 않게 고쳤다(1단계도 함께 고쳐졌다).

> **교훈**: J-N5(화면 문구 ↔ 수치 무모순)는 **문구뿐 아니라 그림에도** 적용된다. 상태가 0·소진·비움에
> 닿는 화면은 그 극한에서 그림이 정말 0으로 보이는지 확인하라 — 보기 좋으라고 넣은 최소 두께·최소
> 크기 매핑이 극한에서 거짓말을 한다.

## ㉙ 점유된 검증 포트 — 죽이지 말고 «무엇을 서빙하는지» 확인하라 (2026-08-24)

검증 서버를 띄우려니 8765를 다른 python이 물고 있었다. ⑮의 교훈(죽은 서버가 검사 실패로 위장)만
기억하고 반사적으로 죽였다면 **같은 저장소를 검증 중이던 다른 세션의 서버**를 끊을 뻔했다.
① 내 서버는 다른 포트(8766)로 띄워 작업하고, ② `검토.js`(8765 고정·공용이라 수정 금지)를 돌리기
전에는 기존 8765 서버가 서빙하는 파일에서 **이번 세션에 새로 넣은 문자열**(`initM3D`)을 curl로
확인해 — 같은 최신 폴더임이 증명된 뒤에야 — 그 서버를 그대로 썼다.

> **교훈**: 점유 포트는 죽이는 것도 쓰는 것도 **내용 확인 뒤에** 한다. 판별법은 신선도 마커 —
> 이번 세션에 새로 추가한 문자열이 그 포트에서 서빙되는지 한 줄로 확인된다.

## ㉚ FAIL 주입 복구에 git checkout 을 썼다가 미커밋 작업을 통째로 날렸다 (2026-08-25)

주입한 결함을 되돌리려고 `git checkout -- <파일>`을 눌렀다. checkout 은 «주입 직전»이 아니라
**«마지막 커밋»**으로 되돌린다 — 그 파일의 미커밋 수정(그날 작업 전부)이 함께 사라졌다.
대화 컨텍스트에 남아 있던 편집 전문을 순서대로 재적용해 복구했지만, checkout 이 줄끝까지
LF→CRLF 로 바꿔 놓아 계산부 동기 검사(⑩)가 덤으로 깨졌다(줄끝 정규화로 해소).
같은 주입에서 확인용 정규식 `/duoTemp/`가 개명 주입 `duoTempX`에 **부분 문자열로 그대로 매치**돼
주입이 통과로 위장되는 ㉗의 재발도 있었다 — 정의(`const duoTemp = b =>`)와 호출(`duoTemp(`)을
경계까지 보는 정규식으로 고쳤다.

> **교훈**: 주입 전 원본은 **파일 사본(.bak)**으로 보관하고 사본으로 복구한다. 미커밋 수정이 있는
> 파일에 `git checkout --`을 쓰지 마라. 그리고 주입 확인 정규식은 이름의 부분 문자열이 아니라
> **정의+호출을 경계까지** 봐야 주입을 정말로 잡는다.

## ㉛ 화면 픽셀을 재는 검사가 세 가지 이유로 거짓값을 냈다 (2026-08-27 · redox)

미시 화면의 판독성을 「렌더된 픽셀」로 재려 했다. 세 번 연속으로 값이 거짓이었다.

1. **프레임 밖에서 읽었다.** WebGL 캔버스는 `preserveDrawingBuffer:false`라 프레임이 끝나면
   버퍼가 비워진다. `drawImage`를 `await` 뒤에 부르니 원자도 배경도 전부 `rgb(0,0,0)`이 나왔고,
   두 표본의 차가 0이라 「변화 없음」이라는 그럴듯한 실패로 위장됐다.
   → **렌더 함수를 동기로 부른 «직후 같은 프레임»에서** 읽고, 끝나면 raf 루프를 되돌린다.
2. **표본 위치를 화면 비율로 추정했다.** 「비커는 대충 화면의 0.72, 0.66쯤」으로 잡았더니 실제
   비커(투영 0.757, 0.642)를 살짝 빗나가 0이 나왔다. → **씬 객체를 카메라로 투영해** 찾는다.
3. **스캔 폭을 상수(±40px)로 두었다.** 구가 그보다 크면 스캔이 테두리에 닿지 못한다 — 같은 화면이
   1194에서 3.98, 360에서 5.31로 갈렸다. **해상도 의존 아티팩트를 성능 차이로 오독할 뻔했다.**
   → 스캔 폭을 **투영 반지름에서 유도**한다.

> **교훈**: 화면을 재는 검사는 «측정 자체»를 먼저 의심한다. 두 해상도에서 값이 갈리면 그것은
> 대개 화면이 아니라 측정의 문제다. 위치는 투영으로, 크기는 기하에서 유도한다(원칙 13의 연장).

## ㉜ 외부에서 받은 three.js 자산이 세 가지 이유로 안 보였다 (2026-08-27 · redox)

미적 요소(불꽃·기포·가루 색 전이·탁도)를 Codex에 발주해 받았다. 문법·금지 패턴·전역 오염은
전부 깨끗했는데 화면에서는 **가루가 새까맣고 불꽃이 보이지 않았다.**

| 증상 | 원인 | 처방 |
|---|---|---|
| 인스턴스 가루가 항상 검정 | `vertexColors:true`인데 지오메트리에 색 속성이 없어 r147이 정점색을 (0,0,0)으로 두고 인스턴스색과 곱했다 | `vertexColors` 제거 |
| 불꽃이 흰 무대에서 소실 | `AdditiveBlending` — 흰 배경(§5 밝은 무대) 위에서 가산 혼합은 흰색으로 날아간다 | `NormalBlending` |
| 색이 전반적으로 떠 보임 | `setColorAt`은 색공간 변환을 하지 않는데 렌더러는 sRGB 출력이었다 | `convertSRGBToLinear()` |

> **교훈**: 외부 조달 자산은 «통합 전 단독 검증»(§1-3 ④)에서 **우리 무대의 배경색·색공간으로**
> 렌더해 본다. 발주처의 데모 배경(`#f2f5f8`)과 우리 무대(`#ffffff`)가 다르면 그 차이만으로
> 이펙트가 사라진다. 원본은 `.orig.js`로 보존하고 보정은 코드 주석으로 남긴다.

## ㉝ `prefers-reduced-motion`을 「시간 상수화」로 구현해 상태 표현까지 멈췄다 (2026-08-27 · redox)

§10의 「자동 재생 대신 일시정지로 시작」을 지키려고 이펙트에 넘기는 시간을 상수로 고정했다
(`const ft = RM ? 8 : t`). 그런데 그 이펙트들은 내부에서 `dt = time - lastTime`으로 **지수 평활**을
돌고 있었다 — dt가 0이 되어 점화·탁도·기포 계수가 **영원히 0**에 머물렀다.
결과: 동작 줄이기를 켠 기기에서 **불꽃이 켜지지 않고 석회수가 끝까지 맑은데 화면 수치는
「뿌옇게 흐려짐 12/12」**. J-N5(문구↔수치 모순)를 정확히 위반했다.

**이 결함은 검사가 없다는 이유로 두 검토 라운드를 살아남았다.** 42개 검사·프로브 어디에도
reduced-motion 경로가 없었다.

> **교훈**: 「움직임을 줄인다」는 **장식 시간만** 멈추는 것이다. 상태를 향해 수렴하는 값(점화·색
> 전이·탁도)은 실제 시간으로 계속 가야 한다. 이펙트 «안»에서 `state.rm`으로 갈라라.
> 그리고 **접근성·설정 경로(RM·WebGL 차단·`file://`)는 검사에 넣는다** — 처방과 검사를 같이 넣지
> 않으면 다음 라운드에서 또 살아남는다.

## ㉞ 게이트가 「점수가 오르는 방향」으로 뚫렸다 (2026-08-27 · redox)

어두운 무대에서 원자가 배경에 묻히지 않는지 보려고 게이트 둘을 만들었다 — 테두리 대비(탐지)와
원소 쌍 색차(판별). 검토자가 **원자를 배경색으로 칠해** 주입하자, 두 게이트는 울리기는커녕
**점수가 올랐다**(탐지 5.77→7.15, 판별 48.5→52.2). 어두울수록 테두리 보조광을 세게 주도록
설계한 탓에, 배경에 수렴할수록 테두리가 더 밝아졌기 때문이다.

> **교훈**: 새 게이트를 만들면 **「통과시키면 안 되는 것」을 주입해** 본다. 값이 오르는 방향으로
> 뚫려 있으면 그 게이트는 자기가 막겠다는 것을 막지 못한다. 이럴 땐 **반대 방향의 회귀 임계**를
> 따로 건다(redox: 안쪽 색↔배경 ΔE ≥ 8 — 정상 12.0 통과, 배경색 주입 3.8 검출).

## ㉟ 자기검사의 대조군이 「직전 측정이 남긴 전역」을 집었다 (2026-08-27 · redox)

게이트가 실제로 색을 재는지 확인하려고 «배경색으로 칠한 대조 공»을 넣었다. 그 색을
`COL.C = M3D_BG`로 잡았는데, `M3D_BG`는 **직전에 돌린 「완결」 측정이 남긴 벌크 색**이었다.
대조 공이 배경색이 아니었으므로 대조값 11.0은 실제 탄소 12.0과 1.0 차이뿐 — **자기검사가
아무것도 증명하지 못하는데 「증명했다」고 보고**했다. 그 상태의 배경색을 직접 계산하도록
고치니 3.8이 나왔고(검토자 독립 측정과 일치) 논증력이 3.2배가 됐다.

> **교훈**: 대조군은 «그 측정이 놓이는 상태»에서 값을 직접 계산해 만든다. 전역 변수를 빌리면
> 앞 측정의 잔재를 집는다. 그리고 **자기검사의 임계는 게이트와 같은 값**으로 맞춰라 — 느슨하면
> 조명·셰이더를 올렸을 때 대조군이 게이트를 통과하는데 자기검사는 침묵하는 잠복 구멍이 생긴다.

## ㊱ 「고쳤다」는 보고가 사실이 아니었다 (2026-08-27 · redox)

검토자 지적을 받아 문서의 표 번호와 건수를 고치는 치환을 돌렸다. 치환 문자열이 실제 본문과
맞지 않아 **아무것도 바뀌지 않았는데** 「정정했다」고 보고했다. 다음 라운드에서 검토자가 파일을
직접 열어 「표가 여전히 `1 2 3 4 6 5`, 「5건」이 두 곳에 그대로」라고 잡았다.

같은 라운드에서 **검토 중에 산출물이 바뀌어 검토가 무효가 된 일이 세 번** 있었다. 검토자의
판정기까지 낡아 오탐 2건이 났고, 검토자가 「다음 호출 전 판을 고정하라」고 요구했다.

> **교훈**: **치환·수정은 결과를 되읽어 확인한 것만 보고에 쓴다**(`grep`으로 잔존 0건을 본다).
> 「검증하지 않은 것」에 적용하는 규율이 「고쳤다」에도 그대로 적용된다.
> 그리고 **검토자를 부르기 전에 판을 고정하고 md5를 함께 준다** — 검토 중에는 산출물을
> 건드리지 않는다. 검토 기록은 파일에 누적시켜 인스턴스가 사라져도 다음 인스턴스가 승계하게 한다.

---

# 5부 · 복사용 프롬프트 블록 v1.0

새 시뮬레이션을 만들 때 아래를 통째로 복사해 프롬프트 앞에 붙이고, 뒤에 "무엇을 만들지"만 적는다.

```
[🔬 화학 탐구실 — 시뮬레이션 작업 매뉴얼 v1.0 요약]

이 시뮬레이션은 '화학 탐구실' 사이트의 한 페이지로 들어간다. 아래를 지킨다.

■ 순서 — 반드시 이 순서로
  ① P0 설계 게이트: 로드맵에서 ★ 오개념을 찾아 "무엇을 깰 것인가"와
     "학생이 직접 조작해서 그것을 깨는 장치"를 먼저 정한다.
     ⚠ 로드맵이 이 도구의 목적을 한정해 두었는지 반드시 확인한다.
  ② P1 수치: 화면을 만들기 전에 계산 함수만 짜서 Node 로 "아는 정답"과 대조.
     최소 5항목. 라이브 수렴성(흔들림·응답 시간)도 잰다.
  ③ 화면을 만든다.
  ④ P2 정적 → ⑤ P3 렌더링 실측 → ⑥ P4-B 스크린샷 육안 확인(생략 금지)
  ⑦ P5 오개념 표 · P6 색각 ΔE → ⑧ P7 은 교사에게 넘긴다(AI 판정 금지).

■ 기술 제약 — 타협 불가
  · 프레임워크·빌드 도구·npm·외부 CDN 금지. 순수 HTML+CSS+JS+Canvas(+WebGL).
  · fetch()·ES 모듈·type="module"·localStorage 금지. 일반 <script src> 만.
  · 폴더 하나에 index.html + sim.js. 화면과 물리를 섞지 않는다.
  · 페이지 맨 아래 4줄 고정:
      <script src="../sims.js"></script>
      <script src="../shared/nav.js" data-base=".." data-current="새id"></script>
      <script src="../shared/qr.js" data-base=".." data-current="새id"></script>
      <script src="sim.js"></script>
  · WebGL 을 쓰면 반드시 폴백 안내문을 두고, 측정값·그래프는 그대로 쓸 수 있게 한다.

■ 레이아웃
  · 100vh·position:fixed·overflow:hidden 금지. 위에서 아래로 읽는 문서.
  · 세로 순서: 제목 → 좌석번호 → (탭) → [무대 | 조절변인·측정값·기록] → 그래프(전체 폭) → 활동지
  · 1024px 미만에서 1단. 1단이 되어도 순서는 무대 → 조절 변인 → 측정값.
    ★ 그래프는 2단 격자 바깥에 둔다(안에 넣으면 1단에서 순서가 깨진다).

■ 색
  · 페이지에서 :root 를 재선언하지 않는다. shared/style.css 토큰만 쓴다.
  · 캔버스 색은 getComputedStyle 로 토큰을 읽어 쓴다.
  · 어두운 무대 --p-* / 밝은 무대 --d-*. 섞지 않는다.
  · ★ 한 무대에 데이터 색 4개 이상 금지. 3개로 줄이거나 두 번째 채널(모양·레이블)을 준다.
    실측: 파랑#1d4ed8↔보라#6d28d9 는 녹색약에서 ΔE 0.4 로 같은 색이다.
  · CPK 원자색과 물질의 실제 색은 하드코딩이 정상이다(토큰으로 바꾸지 말 것).

■ 조작 요소
  · 슬라이더에 물리량별 클래스(rng-temp/vol/count/time)를 준다.
  · 계산한 값을 넣어야 하면 <input type="number"> 를 함께 둔다(16px 이상).
  · 체크박스는 <label> 로 감싸고 그 label 에 min-height:44px.
  · 페이지 <style> 에서 요소 선택자(button{}, input{})를 쓰지 않는다. 클래스로 좁힌다.
  · QR 코드 버튼은 페이지에서 손대지 않는다 — shared/qr.js 를 4번째 줄로 로드하면 제목 옆에 자동으로 붙는다.

■ 정직성
  · 보이는 것이 곧 계산 결과여야 한다. 예쁘게 보이려고 값을 보정하지 않는다.
  · 근사·가정·무시한 항·과장한 배율을 활동지 「이 모형의 가정과 한계」에 번호를 매겨 적는다.
  · 측정에 시간이 걸리는 값은 "재는 중"을 표시하고 그동안 기록을 거절한다.
  · 첫 화면이 이미 볼 만한 상태여야 한다. 빈 무대·빈 그래프로 시작하지 않는다.
  · 활동지 5~9문항, 마지막은 반드시 "이 모형이 실제와 다른 점".

■ 측정·검증 (v1.1)
  · 자동 검증의 "통과"를 믿기 전에 스크립트가 무엇을 실패로 세는지 확인한다(4부 ⑪).
  · ΔE 집합은 손으로 세지 않는다 — 도구가 스캔하는 방식대로 뽑는다(4부 ⑩).
  · 각본·체크리스트의 수치는 계산 코드로 재생성한다(4부 ⑬).
  · 시간이 흐르는 시나리오는 종료 상태까지 검산한다(4부 ⑫).
  · 하나의 표시 판정은 한 곳에서. measureText는 굵은 글꼴 기준(4부 ⑭).
  · 테스트 서버 포트 충돌을 먼저 확인한다(4부 ⑮).
  · 검사를 추가하면 통과+실패 합계가 추가 개수만큼 늘었는지 확인한다(4부 ㉑).
  · 안내·결론 문구는 모든 조작 경로에서 화면 수치와 모순되지 않는지 본다(4부 ㉒ · J-N5).
  · 시나리오는 종단 상태까지 계산 코어로 검산한다(4부 ㉓).

■ 단계식 공개·정답 게이팅 (해당 시 · 2부 §13)
  · 표시 여부는 단계×요소 단일 원천 표 하나로. 흩어진 display 대입 금지.
  · 잠금은 DOM input의 .value에 먼저 쓴다(루프가 매 프레임 DOM을 직접 읽는다).
  · 정답 숨김은 캔버스 그리기 자체를 스킵. 누출 경로는 소스 전수 탐색으로 목록화(위치·좌표 포함).
  · 화학 표현 5대 금지(J-N1~J-N5)를 학생에게 닿는 모든 문자열에 적용한다(1부 P5).

■ 입자 모형 (해당 시 · 2부 §14)
  · 거시 관찰이 기본 화면, 분자 화면은 버튼으로 전환. 두 화면은 같은 모형 상태 하나를 읽는다.
  · 렌더러는 새로 짜지 말고 이식한다 — 공-막대 3D는 liquid/의 M3D(원본 mineral 프로그램 ②)를 복사.
    같은 셰이더로 되는 변형은 uniform 추가 + viewport/scissor 다중 렌더(컨텍스트 증설 금지).
  · 평형은 지수 해로 수렴(진동 없음), 평형에서도 교환을 계속 그리고(M3),
    평형 결론 문구는 지금 실제로 평형인 순간에만 띄운다(J-N5).
  · FAIL 주입은 어떤 검사가 걸렸는지까지 확인한다(4부 ㉗). 극한(0·소진)에서 그림이 수치와
    모순되지 않는지 본다(4부 ㉘).
```

---

# 부록 A · 파일 색인

## 저장소 안 (`화학탐구실/` = GitHub 공개 저장소 루트)

| 파일 | 용도 |
|---|---|
| **`시뮬레이션_작업매뉴얼_v1.0.md`** | **이 문서.** 새 시뮬레이션 제작의 유일한 기준 |
| `디자인매뉴얼_v4.0.md` | 이력 보존용. 내용이 다르면 이 문서가 이긴다 |
| `README.md` | 폴더 규약, 페이지 형태, 추가법, 검증 결과 |
| `로드맵.md` | 사용자용 12주 학습 계획, 데이터 저장 3선택지 |
| `sims.js` | ★ 카탈로그. 새 시뮬레이션 추가 시 유일하게 반드시 고치는 파일 |
| `shared/style.css` | 공통 디자인 + 토큰. `--accent` 한 줄로 사이트 전체 색 변경 |
| `shared/nav.js` | 상단 이동 메뉴 자동 삽입 |
| `shared/qr.js` | QR 코드 버튼 — 자체 구현 인코더(CDN 금지 원칙 준수) + 팝업 UI. `PUBLISHED_BASE` 상수만 배포 주소 바뀌면 고친다 |

## 참고 구현 (새로 만들 때 복사할 것)

| 폴더 | 무엇의 본보기인가 |
|---|---|
| `gaslaws/` | **탭 4개 + 변인 잠금** 구조 · 이상 기체 모형 · "재는 중" 처리 |
| `press/` | **도전 게임**(목표·1회 제출·채점) · 숫자 입력칸 · 막다른 선택 안내 |
| `liquid/` | **WebGL + 폴백** · 시간에 따른 실험(가열 곡선) · 두 그래프 전환 · **5단계 점진 공개 + 정답 게이팅 + 나란히 비교**(세션 11 · §13 본보기) · **거시↔분자 전환 + 닫힌 용기 동적 평형 + 공-막대 이식판 `M3D`**(2026-08-24 · §14 본보기) |
| `mineral/` | **WebGL 3D 물체** · 확대에 따른 화면 전환 · 캔버스 클릭 → 정보 표시 · **공-막대 3D 렌더러의 원본**(이식 전례 §14-②) |
| `vapor/` | 동적 평형을 확률 규칙으로 구현. 평형에서 라울 법칙이 저절로 나온다 |
| `entropy/` | 조합론 계산 + 3탭 전환 |
| `gas/` | 입자 물리 엔진 참고 (실제 기체 편차를 교육 자료로 쓰는 예) |

## 저장소 바깥 (`[AI] 시뮬레이션 사이트/`)

| 파일 | 용도 |
|---|---|
| `검증스크립트/` | 3부 참조 |
| `물질과 에너지/`, `용인고등학교/물질과에너지/ai자료/` | **단원 수업설계 로드맵.** 오개념 위험도 지도의 원천 |
| `배포하기.md` | GitHub 배포 전 과정 + 오류별 대처 |
| `인수인계_v1~vN.md` | 세션 기록 |

# 부록 B · 이 문서의 수치는 어디서 왔나

- **대비비**: WCAG 2.1 상대휘도 공식으로 직접 계산. 흰 카드와 페이지 배경 양쪽에서 재고 낮은 쪽을 기재.
- **색각 ΔE**: Viénot–Brettel–Mollon(1999) 선형 근사로 적·녹·청색약을 시뮬레이션한 뒤
  CIELAB ΔE76 으로 모든 색 쌍의 거리를 계산, 최악값을 기재. (`검토.js` 에 구현돼 있다)
- **성능 수치**(N=300 → 45,000쌍): `gas/sim.js` 의 충돌 판정 구조에서 산출.
- **4부의 함정 30건**: ①~⑨는 세션 1~4, ⑩~⑯은 세션 5(액체 개선·전수 재점검), ⑰~⑳은
  세션 6(mineral 6종→9종 확장·연속 줌·비교 모드), ㉑~㉖은 세션 11(liquid 4단계 점진 공개
  개편), ㉗~㉙는 2026-08-24(liquid 5탭 개편), ㉚은 2026-08-25(피드백 반영 세션)에서
  전부 실제로 발생하고 고친 것.
- ⚠ **아직 실측하지 않은 것**: 이 값들이 **실제 학생 태블릿·빔프로젝터**에서 어떻게 보이는지는
  확인하지 않았다. 실제 기기 프레임률도 잰 적이 없다. 첫 수업 후 관찰 결과로 갱신할 것.
- ⚠ (세션 6 이월) **1023px 미만 화면에서 비교 모드의 프리셋 칩·기호가 좁아진다.** 상하 2단 전환
  또는 안내 문구가 필요하나 이번 개정 범위에서는 동결했다(전달을 막을 결함은 아니라고 판정).
  비교 모드를 손댈 때 §3의 1024px 분기 규칙과 함께 다시 볼 것.
- ⚠ (세션 6 이월) **작은 기호는 임계 픽셀 미만이면 자동으로 숨는 로직이 있다**(예: 4.5px 미만
  비표시 — 얼음 H 기호가 4.4px 로 여기 걸려 안 보인다. 개정 전부터 있던 상태로 이번 회귀는 아니다).
  §6 타이포그래피 규칙에 최소 크기 여유를 두고, 확대·해상도에 따라 임계값에 걸리는 기호가 없는지
  P4-B 에서 함께 확인하라.

# 부록 C · 참고한 외부 설계 원칙

- **PhET Interactive Simulations** — 암묵적 안내(implicit scaffolding). 설명문 없이 인터페이스의
  제약과 행동 유도성만으로 학생을 교육적으로 유의미한 조작으로 이끈다. → §2-④, §3
- **Mayer, 멀티미디어 학습 원리** — 신호·분절·공간 근접·일관성. → §2-⑤⑥, §3
- **WCAG 2.2 SC 2.5.8 Target Size** — 최소 24×24 CSS px. 이 사이트는 학생 태블릿 환경이라
  더 엄격한 44px 적용. → §7
- **Okabe–Ito 색각 안전 팔레트** — 명도·채도가 서로 다른 색을 고르고 혼동이 심한 황록 구간을
  피한다는 설계 사상. 팔레트를 그대로 쓰지는 않고 사상만 가져와 자체 측정으로 재구성. → §9

---

*v1.1 · 2026-07-31 (2026-08-04 갱신: 세션 6 함정 ⑰~⑳ / 2026-08-06 세션 8: §0·§1-3·§1-4 신설 / 2026-08-15 세션 11: P5 J-N 5칙·§13·함정 ㉑~㉖ / 2026-08-17 세션 12: §0-1 제목 짓기 신설 / 2026-08-24 liquid 5탭 개편: §14 입자 모형·함정 ㉗~㉙) · 화학 탐구실*
*다음에도 이 파일(`시뮬레이션_작업매뉴얼_v1.1.md`)에 계속 쌓는다. 새 버전 파일을 새로 만들지 않는다(사용자 지시).*
