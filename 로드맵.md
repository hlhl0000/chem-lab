# 화학 탐구 사이트 만들기 — 교사용 로드맵

> 목표: 자바실험실처럼 학생이 브라우저로 접속해 조작하는 시뮬레이션 사이트 +
> 학생 응답·측정 결과 저장 기능
> 대상: 웹 개발 초보, AI 보조 활용, 학기 중 짬짬이 작업 가능

---

## 0. 큰 그림 먼저

### 무엇을 만드는가

자바실험실의 시뮬레이션은 이름과 달리 지금은 Java가 아닙니다. Java 애플릿은 2017~2021년 사이
모든 주요 브라우저에서 지원이 끊겼기 때문에, 지금 브라우저에서 도는 교육용 시뮬레이션은
사실상 전부 **HTML5 + JavaScript + Canvas** 조합입니다. 즉 당신이 배워야 할 것은 Java가 아니라
**JavaScript 하나**입니다. 이건 좋은 소식입니다.

시뮬레이션 페이지 하나의 구조는 이렇게 단순합니다.

```
HTML   — 슬라이더, 버튼, 캔버스 같은 화면 요소를 배치
CSS    — 그 요소들의 색·간격·배치를 지정
JS     — ① 물리/화학 계산  ② 계산 결과를 캔버스에 그리기  ③ 이 둘을 초당 60번 반복
```

동봉한 `index.html` 하나를 열어 보면 이 세 덩어리가 그대로 들어 있습니다.
**파일 하나 = 시뮬레이션 하나**이고, 그 파일을 서버에 올리면 그게 곧 웹사이트입니다.

### 난이도의 현실

| 단계 | 내용 | 체감 난이도 |
|---|---|---|
| 시뮬레이션 만들기 | HTML/CSS/JS + Canvas | ★★☆☆☆ AI 보조로 빠르게 가능 |
| 인터넷에 올리기 | GitHub Pages | ★☆☆☆☆ 하루면 충분 |
| 여러 개 묶어 사이트로 | 목록 페이지, 폴더 정리 | ★☆☆☆☆ |
| **학생 데이터 저장** | 데이터베이스 + 인증 | ★★★★☆ **여기가 진짜 벽** |

당신이 "학생 응답 저장이 필요하다"고 하셨으니, 이 로드맵은 4단계까지 갑니다.
다만 **3단계까지 먼저 끝내고 실제 수업에 한 번 써 본 뒤에** 4단계로 가시길 강력히 권합니다.
저장 기능은 없어도 되는 경우가 의외로 많고(뒤에 CSV·구글폼 대안 설명),
학생 개인정보가 들어가는 순간 고려할 것이 급격히 늘어나기 때문입니다.

---

## 1. 준비 (1~2일)

설치할 것은 세 개뿐입니다.

1. **VS Code** — 코드 편집기. 무료. 확장에서 `Live Server`를 설치하면
   HTML 파일 우클릭 → "Open with Live Server"로 저장할 때마다 브라우저가 자동 새로고침됩니다.
2. **Chrome** — 개발자도구(F12)가 당신의 실험 장비입니다.
   `Console` 탭에 뜨는 빨간 글씨가 에러이고, 그 글씨를 그대로 AI에게 붙여넣는 것이
   앞으로 가장 자주 하게 될 디버깅입니다.
3. **GitHub 계정** — 무료. 배포와 버전 관리에 씁니다.

> 처음부터 `git` 명령어를 외울 필요는 없습니다.
> GitHub Desktop(GUI 앱)으로 시작해도 되고, 익숙해진 뒤 명령어로 넘어가면 됩니다.

---

## 2. JavaScript 최소 학습 (2~4주, 하루 30분)

전부 배우려 하지 마세요. **시뮬레이션에 실제로 쓰이는 것만** 추립니다.

### 꼭 필요한 것

- 변수 (`let`, `const`), 숫자 연산, `Math.sqrt` / `Math.sin` / `Math.random`
- 조건문 `if`, 반복문 `for`
- 배열과 객체 — 입자 하나가 `{x, y, vx, vy}` 객체이고, 입자 200개가 배열입니다
- 함수 정의와 호출
- **DOM 조작** — `document.getElementById("슬라이더").value` 로 슬라이더 값을 읽기
- **이벤트** — `버튼.onclick = () => { ... }`
- **Canvas 2D** — `ctx.beginPath()`, `ctx.arc()`, `ctx.fill()`, `ctx.fillRect()`
- **애니메이션 루프** — `requestAnimationFrame(loop)`

### 당분간 몰라도 되는 것

React, TypeScript, Node.js, npm, 웹팩/Vite, CSS 프레임워크.
전부 나중에 필요해지면 그때 배우면 됩니다. 지금 손대면 진도가 안 나갑니다.

### 추천 자료

| 자료 | 용도 |
|---|---|
| MDN Web Docs (한국어) — "Canvas API 자습서" | 캔버스 문법의 표준 레퍼런스. 검색해서 필요할 때 찾아보기 |
| freeCodeCamp — JavaScript Algorithms and Data Structures | 문법 기초를 손으로 익히기 |
| PhET Interactive Simulations 소스 | 교육용 시뮬레이션의 세계 표준. 무엇을 만들지 아이디어 얻기 |
| The Coding Train (YouTube) | 캔버스 애니메이션·물리 시뮬레이션 감각 익히기 |

**핵심은 "완주"가 아니라 "동봉한 `index.html`을 읽고 이해하기"입니다.**
그 파일의 `step()` 함수 한 줄 한 줄이 무슨 뜻인지 설명할 수 있게 되면 2단계는 끝난 겁니다.

---

## 3. AI를 제대로 쓰는 법

이 부분이 로드맵에서 가장 중요합니다. AI를 잘못 쓰면 코드는 늘어나는데 실력은 안 늘고,
어느 순간 자기 사이트를 자기가 고칠 수 없게 됩니다.

### 원칙 1 — 물리를 먼저, 코드를 나중에

나쁜 요청: "산-염기 적정 시뮬레이션 만들어줘"
좋은 요청:

> 0.1 M HCl 25 mL에 0.1 M NaOH를 적하하는 적정 곡선을 그리려 합니다.
> 강산-강염기이므로 당량점 전에는 남은 H⁺ 농도로, 후에는 남은 OH⁻ 농도로 pH를 계산하고,
> 당량점에서는 7이 됩니다. 물의 자체 이온화는 무시합니다.
> 이 계산을 하는 JavaScript 함수 `pH(적하부피)`를 먼저 작성해 주세요. 그리기는 그다음에 합니다.

**당신은 화학 전문가이고 AI는 코딩 도구입니다.** 모델의 가정(무시할 항, 근사, 단위)을
당신이 지정하지 않으면 AI가 임의로 정하고, 그 결과가 교과서와 미묘하게 어긋납니다.
학생에게 틀린 걸 보여주는 게 가장 나쁜 결과입니다.

### 원칙 2 — 검증을 요청하라

시뮬레이션은 "그럴듯하게 움직이는 것"과 "맞는 것"이 다릅니다.
반드시 아는 정답으로 확인하세요.

> 이 시뮬레이션이 보일 법칙을 만족하는지 확인하고 싶습니다.
> 화면 없이 계산만 도는 테스트 스크립트를 만들어서, 온도를 고정하고 부피를 5단계로 바꿨을 때
> P·V 값이 실제로 일정한지 숫자로 출력해 주세요.

동봉한 시뮬레이션도 이 방식으로 검증했습니다. 그 결과 게이뤼삭 법칙과 그레이엄 법칙은
오차 2% 이내로 성립했지만, 보일·아보가드로 법칙은 입자에 크기가 있어서 2~35% 벗어났습니다.
**이걸 몰랐으면 학생이 "왜 PV가 일정하지 않죠?"라고 물었을 때 답할 수 없었을 겁니다.**
(지금은 반데르발스 b항을 가르치는 심화 활동으로 바꿔 넣었습니다.)

### 원칙 3 — 설명을 요구하라

코드를 받은 뒤 항상 한 번 더 물으세요.

> 방금 준 코드에서 `thermostat()` 함수가 왜 필요한지, 없으면 어떤 일이 생기는지 설명해 주세요.
> 그리고 `lambda` 계산식이 왜 제곱근인가요?

이 질문 하나가 "복붙"과 "학습"을 가릅니다. 다음 시뮬레이션은 절반쯤 혼자 만들 수 있게 됩니다.

### 원칙 4 — 한 번에 하나씩

"시뮬레이션 5개랑 로그인이랑 대시보드 만들어줘"는 실패합니다.
기능 하나 추가 → 브라우저에서 확인 → 저장 → 다음 기능. 이 리듬을 지키세요.
동작하는 상태를 GitHub에 자주 저장해 두면 망가져도 되돌릴 수 있습니다.

### 원칙 5 — 에러는 그대로 붙여넣어라

F12 콘솔의 빨간 글씨 전체를 복사해서 그대로 주세요. "안 돼요"는 정보가 없습니다.

---

## 4. 배포 — GitHub Pages (반나절)

무료, 무제한 트래픽, HTTPS 자동, 학교 방화벽에서도 대체로 열립니다. 정적 사이트에는 최선의 선택입니다.

1. GitHub에서 저장소(repository) 생성 — 이름 예: `chem-lab`, **Public**으로
2. `index.html`을 업로드 (웹 화면에서 드래그 앤 드롭으로도 됩니다)
3. 저장소 → **Settings → Pages → Source: Deploy from a branch → main / (root)** 선택 → Save
4. 1~2분 뒤 `https://<아이디>.github.io/chem-lab/` 에서 열립니다

폴더 구조는 이렇게 키워 나가면 됩니다.

```
chem-lab/
├── index.html            ← 시뮬레이션 목록 (사이트 대문)
├── gas/index.html        ← 기체 분자 운동
├── titration/index.html  ← 산-염기 적정
├── equilibrium/index.html← 화학 평형
└── shared/
    ├── style.css         ← 공통 디자인
    └── util.js           ← 공통 함수
```

주소는 `.../chem-lab/gas/` 처럼 됩니다. 학생에게는 QR코드로 배포하면 편합니다.

> **학교 환경 확인**: 배포 후 반드시 학교 와이파이의 학생 크롬북/태블릿에서 직접 열어 보세요.
> 교육청 필터링에 github.io가 막혀 있는 곳이 간혹 있습니다.
> 막혔다면 무료 대안으로 Netlify, Cloudflare Pages, Vercel이 있고, 도메인만 다를 뿐 절차는 비슷합니다.

---

## 5. 학생 데이터 저장 — 3가지 선택지

여기서 갈림길입니다. **위에서부터 검토해서, 되는 가장 단순한 것을 고르세요.**

### 선택지 A — CSV 다운로드 후 제출 (난이도 ★☆☆☆☆)

동봉한 시뮬레이션에 이미 들어 있는 방식입니다. 학생이 [CSV로 저장]을 누르면
`홍길동_기체분자운동.csv` 파일이 다운로드되고, 그걸 클래스룸이나 e학습터에 제출합니다.

- **서버·데이터베이스·개인정보 처리 전부 불필요**
- 교사는 받은 CSV를 엑셀에서 한 번에 열어 채점
- 단점: 학생이 파일을 잃어버림, 실시간 현황 파악 불가

**대부분의 수업에는 이걸로 충분합니다.** 첫 학기는 이걸로 운영해 보시길 권합니다.

### 선택지 B — 구글 폼 / 구글 스프레드시트 연동 (난이도 ★★☆☆☆)

시뮬레이션 페이지 안에서 측정값을 구글 스프레드시트로 바로 전송합니다.
Google Apps Script로 웹앱을 하나 만들고, 페이지에서 `fetch()`로 쏘는 방식입니다.

- 학교가 이미 구글 워크스페이스를 쓴다면 계정·권한 문제가 없음
- 결과가 스프레드시트에 실시간으로 쌓여서 수업 중 진행 상황 확인 가능
- 코드 20~30줄 수준

가장 **비용 대비 효율이 좋은 지점**입니다. 저장이 꼭 필요하다면 여기서 시작하세요.

### 선택지 C — Supabase (난이도 ★★★★☆)

진짜 백엔드입니다. PostgreSQL 데이터베이스 + 로그인 + API를 무료 티어로 제공합니다.
교사 대시보드, 반별 집계, 재접속 시 이어하기 같은 게 필요할 때 갑니다.

**최소 스키마 예시**

```sql
create table submissions (
  id          bigserial primary key,
  student_id  uuid references auth.users(id),   -- 로그인한 학생
  sim_name    text not null,                    -- 'gas', 'titration' ...
  class_name  text,                             -- '1-3'
  payload     jsonb not null,                   -- 측정값 배열 통째로
  created_at  timestamptz default now()
);
```

**반드시 알아야 할 함정 — RLS(Row Level Security)**

Supabase의 anon key는 브라우저 코드에 노출됩니다(원래 그렇게 설계된 것).
그래서 테이블에 RLS 정책을 켜지 않으면 **학생이 개발자도구로 다른 학생의 답을 전부 읽거나
지울 수 있습니다.** 초보자가 가장 자주 사고 내는 지점입니다.

```sql
alter table submissions enable row level security;

-- 학생은 자기 것만 넣을 수 있다
create policy "insert own" on submissions
  for insert with check (auth.uid() = student_id);

-- 학생은 자기 것만 읽을 수 있다
create policy "read own" on submissions
  for select using (auth.uid() = student_id);
```

교사 조회는 별도 역할이나 서버 측 키로 처리합니다.
**이 부분은 반드시 AI에게 "RLS 정책이 제대로 걸렸는지 검토해 달라"고 따로 요청하고,
직접 다른 계정으로 로그인해서 남의 데이터가 안 보이는지 확인하세요.**

### 개인정보에 관하여

학생 이름·학번을 저장하는 순간 학교의 개인정보 처리 절차 대상이 될 수 있습니다.
실제 운영 전에 학교 정보부장 또는 개인정보 보호 담당자에게 확인하세요.
**실무적인 회피책**: 이름 대신 **좌석번호나 익명 코드**만 저장하고,
코드↔이름 대응표는 교사가 따로 보관하면 위험이 크게 줄어듭니다.
선택지 A(CSV)는 애초에 학교 서버 밖으로 데이터가 나가지 않아 이 문제에서 자유롭습니다.

---

## 6. 12주 실행 계획

| 주차 | 할 일 | 완료 기준 |
|---|---|---|
| 1 | VS Code·Chrome·GitHub 설치, 동봉 파일 열어 보기 | 내 컴퓨터에서 시뮬레이션이 돈다 |
| 2–3 | JavaScript 기초 (변수·배열·함수·DOM) | 슬라이더 값을 화면에 표시하는 페이지를 혼자 만든다 |
| 4 | Canvas 기초 | 원 하나가 벽에 튕기며 움직이는 페이지를 혼자 만든다 |
| 5 | **동봉 `index.html` 정독** | `step()`, `thermostat()`, `drawHist()`가 무슨 일을 하는지 설명할 수 있다 |
| 6 | GitHub Pages 배포 | 학교 와이파이의 학생 기기에서 주소로 접속된다 |
| 7–8 | 두 번째 시뮬레이션 (AI 보조로) — 산-염기 적정 추천 | 검증 스크립트로 pH 계산이 맞는지 확인 완료 |
| 9 | 목록 페이지·공통 CSS 정리 | 사이트 형태를 갖춤 |
| 10 | **실제 수업에 1차시 투입 (CSV 제출 방식)** | 학생 피드백 확보 |
| 11–12 | 피드백 반영 + 필요하면 구글 시트 연동 | 2차시 운영 |

Supabase는 이 12주가 끝난 뒤, 정말 필요하다고 판단되면 시작하세요.

---

## 7. 다음에 만들면 좋을 시뮬레이션

교육과정 연계와 구현 난이도를 함께 고려한 순서입니다.

1. **산-염기 적정 곡선** — 계산이 단순하고 그래프가 강력. 지시약 색변화까지 넣으면 완성도가 높음
2. **화학 평형 / 르샤틀리에** — 농도·온도·압력 변화에 따른 이동. 막대그래프로 표현
3. **반응 속도와 활성화 에너지** — 지금 만든 속력 분포 그래프를 그대로 재활용 가능
4. **원자 오비탈 / 전자 배치** — 3D는 어려우므로 2D 단면 확률밀도부터
5. **결합의 극성과 쌍극자** — 전기음성도 슬라이더로 전자구름 치우침 표현
6. **용해도 곡선과 재결정** — 온도에 따른 석출량 계산

---

## 8. 초보자가 자주 빠지는 함정

- **처음부터 React를 배우려 한다** → 시뮬레이션 사이트에는 필요 없습니다. 순수 HTML/JS로 충분합니다.
- **AI가 준 코드를 검증 없이 학생에게 보여준다** → 반드시 아는 정답으로 확인하세요. 3장 원칙 2.
- **한 파일에 시뮬레이션 10개를 넣는다** → 페이지 하나에 시뮬레이션 하나. 폴더로 나누세요.
- **모바일 확인을 안 한다** → 학생 절반은 휴대폰으로 봅니다. Chrome F12의 기기 모드로 확인하세요.
- **저장 없이 계속 고친다** → 동작할 때마다 GitHub에 커밋. 망가지면 되돌릴 수 있습니다.
- **API 키를 코드에 그대로 넣는다** → Supabase anon key는 괜찮지만(RLS 전제),
  service_role key는 절대 브라우저 코드에 넣으면 안 됩니다.
- **완벽한 사이트를 만든 뒤 공개하려 한다** → 시뮬레이션 하나로 수업 한 번 해 보는 게
  석 달 혼자 만드는 것보다 훨씬 많은 걸 알려줍니다.

---

## 지금 바로 할 일

1. 동봉한 `index.html`을 더블클릭해서 브라우저로 열어 보세요. 설치 없이 바로 돕니다.
2. 슬라이더를 움직이며 활동지 1~6번을 직접 풀어 보세요. 수업에 쓸 만한지 판단이 설 겁니다.
3. VS Code로 그 파일을 열고 `const H = 300;`을 `500`으로 바꿔 저장한 뒤 새로고침해 보세요.
   **코드를 고쳐서 화면이 바뀌는 경험**이 시작점입니다.
